# 电商用户行为分析与智能推荐平台

## 全项目全流程开发规格说明书（V2.2 基于 Phase 0\~3 已完成版）

> **用途**：本文件专门交给代码生成
> LLM，作为项目的总体开发规格、功能边界、技术路线、数据模型、算法要求、API
> 设计、前端设计、测试标准和迭代路线。
>
> **项目定位**：面向秋招求职展示的综合型 Python
> 数据分析、机器学习、推荐系统与数据应用平台。
>
> **核心原则**：项目不是简单的"Pandas 数据分析作业"，也不是单纯
> CRUD。最终需要形成完整的数据闭环：
>
> **数据生成/采集 → 数据清洗 → ETL → 数据存储 → 数据质量 → 基础分析 →
> 深度业务分析 → 用户画像 → 商品画像 → 用户分群 → 预测建模 → 推荐系统 →
> 推荐评估 → FastAPI → Vue3/ECharts → 测试 → 部署 → 项目文档 →
> 秋招面试材料**
>
> 本文档按照"先完成稳定 MVP，再逐层增强"的方式设计。代码生成 LLM
> **禁止一次性生成整个项目**，必须严格按照 Phase
> 顺序开发、运行、测试和修复。

------------------------------------------------------------------------

## V2.2 版本说明：基于当前实际开发进度继续，而不是重新规划

### 2.2.1 当前开发状态（非常重要）

**当前项目已经完成 Phase 0、Phase 1、Phase 2、Phase 3。**

这里的 **Phase 0\~3 是开发阶段编号**，不是本文后面的 P0/P1/P2 优先级。

当前状态：

  -----------------------------------------------------------------------------------------
  开发阶段                状态                    含义
  ----------------------- ----------------------- -----------------------------------------
  Phase 0                 ✅ 已完成               需求、总体架构、项目目录、基础文档

  Phase 1                 ✅ 已完成               Python + FastAPI
                                                  初始化、配置、日志、数据库连接、Health
                                                  API

  Phase 2                 ✅ 已完成               MySQL 核心表结构、约束、索引

  Phase 3                 ✅ 已完成               具有业务规律的数据生成器

  Phase 4                 ▶️ 下一阶段             数据质量 + ETL

  Phase 5 以后            ⏳ 待开发               分析、建模、推荐、API、前端、测试、部署
  -----------------------------------------------------------------------------------------

> **代码生成 LLM 必须把当前仓库视为已有项目。禁止重新生成 Phase
> 0\~3，禁止因为发现目录或代码风格可以优化就整体重写。后续开发必须在现有实现基础上增量修改。**

### 2.2.2 V2.2 的核心修改

V2.1 的总体方向正确，本版本不推翻原有技术路线，只针对"已经开发到 Phase
3"这一事实进行工程化调整：

1.  **冻结 Phase 0\~3**
    -   Phase 0\~3 视为现有基线。
    -   后续 LLM 默认这些模块已经存在。
    -   如发现问题，先做兼容性修复，不得借机重构整个项目。
2.  **从 Phase 4 开始继续开发**
    -   下一步直接进入数据质量与 ETL。
    -   所有新模块必须复用现有数据生成器和数据库结构。
    -   如必须修改已有表结构，必须先说明迁移原因、影响范围和兼容方案。
3.  **消除"P0"和"Phase 0"概念混淆**
    -   `Phase 0~19`：开发阶段。
    -   `P0/P1/P2`：功能优先级。
    -   两套编号不得混用。
4.  **增加数据事实层 / 维度层 / 派生层概念**
    -   明确哪些是原始事实数据。
    -   哪些是用户、商品等维度数据。
    -   哪些是 RFM、画像、特征、推荐结果等派生数据。
5.  **预测任务增加严格时间窗口**
    -   Observation Window：用于生成特征。
    -   Prediction Window：用于生成标签。
    -   禁止特征窗口和标签窗口发生时间穿越。
6.  **推荐系统增加实验思维**
    -   行为权重必须配置化。
    -   Hybrid 权重必须通过离线实验比较。
    -   推荐算法必须与 Popular Baseline 对比。
    -   推荐效果不能只展示一个"漂亮指标"。
7.  **业务分析从"现象"升级到"现象 → 原因 → 建议"**
    -   但必须注明：数据为模拟数据，结论主要用于验证方法论。
8.  **把推荐系统作为最终项目主线**
    -   数据分析用于理解用户。
    -   用户画像用于形成特征。
    -   预测模型用于理解未来行为。
    -   推荐系统用于最终决策。
    -   FastAPI + Vue3 用于产品化展示。
9.  **增加最终 Demo 闭环**
    -   面试展示时从用户画像进入预测，再进入推荐，再进行算法对比和推荐评估。
    -   让项目从"功能集合"变成一个完整故事。

### 2.2.3 P0/P1/P2 优先级保持不变

-   **P0：核心，必须完成**
-   **P1：加分项，P0 完成且有时间再做**
-   **P2：只做架构设计和面试话术，不实现 Kafka/Spark/Flink**

当前开发重点：

> **Phase 4\~19 是 P0 主线；P1 只能在 P0 完整闭环后追加；P2
> 永远只产出设计文档。**

------------------------------------------------------------------------

------------------------------------------------------------------------

# 0. 项目最终目标

最终项目应该能够回答四类业务问题：

## 0.1 发生了什么？

-   用户有多少？用户什么时候活跃？用户做了什么行为？
-   哪些商品卖得最好？哪些分类表现最好？哪些渠道带来的用户最好？
-   GMV 如何变化？转化漏斗在哪一步流失最严重？

## 0.2 为什么发生？

-   为什么某类用户更容易购买？哪些行为与购买强相关？
-   用户通常经过什么路径完成购买？哪些商品经常一起购买？
-   什么价格区间转化率最高？哪些渠道用户质量最高？
-   哪些用户正在沉默或流失？哪些商品正在进入衰退期？

## 0.3 接下来会发生什么？

-   用户未来 7 天是否可能购买？用户未来 30 天是否可能流失？
-   某用户对某商品的点击概率是多少？哪些用户具有高价值潜力？

## 0.4 应该做什么？

-   应该向用户推荐什么商品？为什么推荐这个商品？
-   不同推荐算法哪个更好？推荐系统覆盖了多少商品？
-   如何针对不同用户群采用不同推荐策略？

------------------------------------------------------------------------

# 1. 项目范围与优先级分级

## P0（核心，必须完成 ------ 决定项目是否成立）

-   数据生成、数据清洗、ETL、数据质量检查
-   用户分析、商品分析、GMV 分析、漏斗分析、留存分析、RFM
-   用户生命周期、用户购买路径、Cohort
    分析、商品生命周期、商品关联规则、价格分析、渠道分析、用户分群、用户画像、商品画像
-   购买预测、流失预测
-   热门推荐、Item-CF、User-CF、内容推荐、Hybrid、冷启动、推荐效果评估
-   FastAPI、Vue3、ECharts、API
    分层、日志、测试、配置管理、Docker（backend/frontend/mysql 三容器）

## P1（加分项，P0 完成且时间允许再做）

-   用户价值预测（回归）
-   CTR 预测
-   多路召回、Learning-to-Rank / LightGBM 排序
-   推荐多样性
-   Redis 缓存（Dashboard、推荐结果、用户画像）

## P2（仅设计与面试话术，不要求实现代码）

-   Kafka 实时链路
-   Spark / Flink 大数据处理

> 判断标准：当前数据规模（用户万级、行为百万级）下，Kafka/Spark/Flink
> 带来的实际收益低于开发和运维成本。这类内容只需要在
> `docs/architecture.md` 中给出**架构设计图 + 技术选型理由 +
> 如果数据量增长 100 倍该如何演进**，作为面试第 40
> 题（"如果数据扩大100倍怎么办？"）的标准答案素材，不需要真实跑起来。

------------------------------------------------------------------------

# 1.1 当前代码基线与增量开发规则

由于 Phase 0\~3 已经完成，后续开发必须遵守以下规则。

## A. 不重新生成已有模块

已有模块包括：

``` text
Phase 0
├── 项目需求
├── 总体架构
├── 项目目录
└── README 基础结构

Phase 1
├── FastAPI
├── config
├── logging
├── database connection
└── /api/health

Phase 2
├── users
├── categories
├── items
├── user_behaviors
├── orders
└── order_items

Phase 3
└── 具有业务规律的数据生成器
```

以上列表是**逻辑基线**。具体文件名、类名、函数名以当前仓库实际代码为准。

## B. 每次修改前必须先检查

LLM 必须先：

``` text
读取当前目录
 ↓
读取相关代码
 ↓
读取数据库 schema
 ↓
确认已有接口
 ↓
确认已有数据格式
 ↓
设计兼容方案
 ↓
再修改
```

禁止：

``` text
“为了方便”直接重写整个项目
```

## C. 如果 Phase 0\~3 存在 Bug

采用：

``` text
严重 Bug
→ 必须修复

影响后续开发的小问题
→ 可以修复

纯代码风格问题
→ 非必要不重构

与当前需求无关的架构升级
→ 暂不处理
```

## D. 新代码必须兼容已有数据生成器

Phase 4 之后所有分析都应该能够直接消费 Phase 3 已生成的数据。

禁止为了分析方便重新设计一套完全不同的数据生成逻辑。

## E. 数据版本

从 Phase 4 开始建议记录：

``` text
dataset_version
data_start_time
data_end_time
generator_version
etl_version
```

例如：

``` text
dataset_version = v1
generator_version = v1.0
etl_version = v1.0
```

如果后续重新生成数据，必须说明版本变化。

------------------------------------------------------------------------

# 2. 系统总体架构（P0+P1，落地实现范围）

``` text
                         ┌──────────────────────┐
                         │   用户行为/订单数据   │
                         └──────────┬───────────┘
                                    │
                         数据生成 / 数据采集
                                    ▼
                         ┌──────────────────────┐
                         │       Raw Data       │
                         └──────────┬───────────┘
                                    │
                              数据质量检查
                                    ▼
                         ┌──────────────────────┐
                         │ Cleaning / ETL       │
                         └──────────┬───────────┘
                                    ▼
                              MySQL 数据仓库
                                    ▼
                         ┌──────────────────────┐
                         │    数据分析层        │
                         │ 用户/商品/GMV/漏斗/  │
                         │ 留存/Cohort/RFM/路径 │
                         │ /生命周期/关联/渠道  │
                         └──────────┬───────────┘
                                    ▼
                         ┌──────────────────────┐
                         │      特征工程层       │
                         └──────────┬───────────┘
                   ┌────────────────┴────────────────┐
                   ▼                                 ▼
          ┌──────────────────┐             ┌──────────────────┐
          │ 预测模型(购买/   │             │ 推荐系统         │
          │ 流失，P1:CTR/价值)│             │ Popular/ItemCF/  │
          └────────┬─────────┘             │ UserCF/Content/  │
                   │                       │ Hybrid(P1:多路召回/排序)│
                   │                       └────────┬─────────┘
                   └────────────────┬──────────────┘
                                    ▼
                              模型评估层
                                    ▼
                              FastAPI Service
                          ┌─────────┴─────────┐
                          ▼                   ▼
                       MySQL          Redis(P1，可选)
                          └─────────┬─────────┘
                                    ▼
                              Vue3 + ECharts
                                    ▼
                              数据分析平台
```

## 2.1 扩展架构（P2，仅设计不实现）

``` text
Frontend / Simulator → Kafka → Consumer → Redis → FastAPI → Dashboard
```

用于支撑实时 PV、实时活跃、实时热门商品、实时 GMV。Spark/Flink
用于数据量达到千万级后的离线/实时大规模处理。**这部分只写方案，不写实现代码。**

------------------------------------------------------------------------

# 3. 技术栈

## 3.1 Python（P0）

-   Python 3.11+、Pandas、NumPy、SciPy、scikit-learn、mlxtend、joblib
-   PyMySQL、SQLAlchemy 2.x、Pydantic、FastAPI、Uvicorn

## 3.2 Python（P1 可选）

-   LightGBM、XGBoost

## 3.3 数据库（P0）

-   MySQL 8.x

## 3.4 缓存（P1）

-   Redis

## 3.5 消息队列 / 大数据（P2，仅设计）

-   Kafka、Spark、Flink

## 3.6 前端（P0）

-   Vue 3、Vite、TypeScript、Axios、Vue Router、ECharts、Element Plus

## 3.7 工程（P0）

-   Git、pytest、Docker、Docker Compose、Ruff、Black

------------------------------------------------------------------------

# 4. 项目目录

``` text
ecommerce-recommendation-platform/
│
├── backend/
│   ├── app/
│   │   ├── main.py
│   │   ├── core/{config.py,database.py,logging.py,exceptions.py}
│   │   ├── models/ ├── schemas/ ├── routers/ ├── services/
│   │   ├── repositories/ └── middleware/
│   ├── tests/
│   ├── requirements.txt
│   └── .env.example
│
├── analysis/
│   ├── data_generation/
│   ├── cleaning/
│   ├── etl/
│   ├── quality/
│   ├── analysis/{user,item,funnel,retention,cohort,path,lifecycle,channel,price,association}/
│   ├── feature_engineering/
│   ├── models/{purchase_prediction,churn_prediction,ctr_prediction(P1)}/
│   ├── recommendation/
│   │   ├── base.py, popular.py, item_cf.py, user_cf.py, content_based.py
│   │   ├── hybrid.py, recall.py(P1), ranking.py(P1), evaluator.py
│   └── requirements.txt
│
├── data/{raw,interim,processed,features}/  + README.md
├── sql/{schema.sql,indexes.sql,views.sql,seed.sql}
│
├── frontend/
│   ├── src/{api,components,views,router,stores,types,utils}/
│   ├── package.json
│   └── vite.config.ts
│
├── tests/
├── docs/{architecture.md,database.md,analysis.md,recommendation.md,machine_learning.md,api.md,interview.md}
├── scripts/{init_db.py,generate_data.py,run_etl.py,run_all.py}
│
├── docker/
├── .env.example
├── .gitignore
├── docker-compose.yml
├── README.md
└── LICENSE
```

------------------------------------------------------------------------

# 5. 数据体系设计

必须包含（P0）：

``` text
users
categories
items
user_behaviors
orders
order_items
```

建议增加（P0，用于画像/分群/推荐落库）：

``` text
user_profile
item_profile
user_segments
recommendation_results
model_predictions
model_metrics
```

------------------------------------------------------------------------

# 5.1 数据分层设计（V2.2 新增）

从 Phase 4 开始，不仅要"把数据放进 MySQL"，还要明确数据属于哪一层。

## 原始事实层

记录用户真实发生的行为或交易：

``` text
user_behaviors
orders
order_items
```

## 维度层

描述业务对象：

``` text
users
items
categories
```

## 派生分析层

由事实数据计算得到：

``` text
user_profile
item_profile
user_segments
```

## 特征层

用于机器学习和推荐：

``` text
user_features
item_features
user_item_features
context_features
```

## 模型/推荐结果层

``` text
model_predictions
model_metrics
recommendation_results
```

### 原则

``` text
原始数据
  ↓
清洗后的事实数据
  ↓
分析指标
  ↓
画像 / 特征
  ↓
模型 / 推荐
```

不要把所有计算结果都直接写回原始事实表。

------------------------------------------------------------------------

# 6. 用户表 users

``` sql
id BIGINT PRIMARY KEY
user_id VARCHAR(32) UNIQUE NOT NULL
age INT
gender VARCHAR(16)
city VARCHAR(64)
register_time DATETIME
created_at DATETIME
updated_at DATETIME
```

要求：user_id 唯一；年龄合理；注册时间合理；城市分布不能全部相同。

------------------------------------------------------------------------

# 7. 分类表 categories

``` sql
id BIGINT PRIMARY KEY
category_id VARCHAR(32) UNIQUE NOT NULL
category_name VARCHAR(128)
parent_id VARCHAR(32)
```

至少生成：手机数码、电脑办公、家用电器、服饰、鞋包、美妆、食品、家居、运动户外、图书。

------------------------------------------------------------------------

# 8. 商品表 items

``` sql
id BIGINT PRIMARY KEY
item_id VARCHAR(32) UNIQUE NOT NULL
item_name VARCHAR(255)
category_id VARCHAR(32)
brand VARCHAR(128)
price DECIMAL(10,2)
stock INT
status TINYINT
created_at DATETIME
```

商品必须有不同热度等级（热门/普通/冷门），且不同分类的商品价格分布不同。

------------------------------------------------------------------------

# 9. 用户行为表 user_behaviors

``` sql
id BIGINT PRIMARY KEY
behavior_id VARCHAR(64) UNIQUE NOT NULL
user_id VARCHAR(32)
item_id VARCHAR(32)
behavior_type VARCHAR(16)
event_time DATETIME
event_date DATE
event_hour TINYINT
device_type VARCHAR(32)
channel VARCHAR(32)
```

-   行为：pv / click / collect / cart / buy
-   渠道：organic / search / ads / campaign / recommendation
-   设备：mobile / pc / tablet

------------------------------------------------------------------------

# 10. 订单表 orders

``` sql
id BIGINT PRIMARY KEY
order_id VARCHAR(64) UNIQUE NOT NULL
user_id VARCHAR(32)
order_time DATETIME
total_amount DECIMAL(12,2)
status VARCHAR(32)
payment_method VARCHAR(32)
```

状态：paid / cancelled / refunded

------------------------------------------------------------------------

# 11. 订单明细 order_items

``` sql
id BIGINT PRIMARY KEY
order_id VARCHAR(64)
item_id VARCHAR(32)
quantity INT
unit_price DECIMAL(10,2)
amount DECIMAL(12,2)
```

------------------------------------------------------------------------

# 12. 数据量规划

建议最终规模：

``` text
用户：10,000        分类：10~30
商品：5,000         用户行为：500,000~2,000,000
订单：50,000~150,000  订单明细：100,000~300,000
```

低配置电脑：

``` text
用户：2,000   商品：1,000   行为：100,000   订单：10,000
```

代码必须支持配置数据量（这也是 Kafka/Spark 被移入 P2
的直接依据------该量级下单机 Pandas/MySQL 已足够）。

------------------------------------------------------------------------

# 13. 模拟数据必须具有业务规律

禁止完全随机生成。

-   **用户偏好**：每个用户随机分配 1\~3 个偏好类别。
-   **商品热度**：热门商品得到更高曝光概率。
-   **行为链**：购买必须与前置行为存在概率关联（PV → Click →
    Collect/Cart → Buy）。
-   **时间规律**：晚间活跃度较高，周末活跃度略高。
-   **用户价值**：至少生成高/中/低价值用户。
-   **渠道差异**：不同渠道设置不同的点击率、转化率、用户质量，渠道分析才有意义。

> 局限性提示（写入
> docs/analysis.md）：所有"业务发现"均来自模拟规则本身，本质上是对生成规则的验证而非真实业务洞察，面试时应如实说明这是模拟数据集，重点展示方法论而非结论本身。

------------------------------------------------------------------------

# 14. 数据质量系统

必须检查：

-   **完整性**：user_id / item_id / event_time 是否为空
-   **唯一性**：behavior_id / order_id 是否重复
-   **一致性**：行为中的 user_id / item_id 是否存在于 users / items
-   **合法性**：behavior_type 是否属于允许集合；price \>= 0；quantity \>
    0
-   **时间**：事件时间不能早于用户注册时间；订单时间不能晚于数据结束时间

输出 `data_quality_report.json`：

``` json
{
  "total_rows": 100000,
  "duplicate_rows": 120,
  "missing_rows": 32,
  "invalid_rows": 15,
  "final_rows": 99833
}
```

------------------------------------------------------------------------

# 15. ETL 流程

``` text
Raw → Schema Validation → Cleaning → Transformation → Quality Check → Feature Generation → MySQL
```

要求：批量插入；支持重复执行；支持输出进度；大文件使用
chunksize；禁止逐行写数据库。

------------------------------------------------------------------------

# 16. 基础数据分析

## 16.1 用户规模

指标：总用户 / 新增用户 / 活跃用户 / 购买用户 / 付费率

## 16.2 DAU / WAU / MAU

``` text
DAU = 当日产生至少一次行为的用户数
WAU = 当周产生行为的去重用户数
MAU = 当月产生行为的去重用户数
```

分析：趋势、环比、同比（模拟数据有意义时）、活跃用户占比

## 16.3 用户行为分析

统计 PV/Click/Collect/Cart/Buy，计算点击率/收藏率/加购率/购买率

## 16.4 活跃时间分析

按 hour / weekday
统计行为量，回答：用户什么时候最活跃？哪些时间购买最多？PC
和移动端活跃时间是否不同？

------------------------------------------------------------------------

# 17. GMV 分析

指标：GMV、订单数、购买用户数、客单价、ARPU、付费率

``` text
GMV = 有效支付订单金额之和
客单价 = GMV / 支付订单数
ARPU = GMV / 活跃用户数
```

按日/周/月分析 GMV 趋势、订单趋势、购买用户趋势、客单价趋势。

------------------------------------------------------------------------

# 18. 商品与分类分析

## 18.1 商品分析

指标：PV/Click/Collect/Cart/Buy/GMV/Unique Users/Conversion Rate；输出
TOP10/TOP20 商品、分类排行、品牌排行。

## 18.2 商品转化率

``` text
click_rate = click / pv
collect_rate = collect / pv
cart_rate = cart / pv
buy_rate = buy / pv
```

所有除法必须防止除零。

## 18.3 分类分析

按分类统计用户数/PV/点击/收藏/加购/购买/订单/GMV/转化率。

## 18.4 品牌分析

按品牌统计销量/GMV/用户数/复购用户/客单价。

------------------------------------------------------------------------

# 19. 转化漏斗

标准漏斗：PV → Click → Collect → Cart → Buy

输出：stage / count / step_conversion_rate /
overall_conversion_rate，需分析每一步损失。

------------------------------------------------------------------------

# 20. 留存与 Cohort

## 20.1 用户留存

实现次日/3日/7日/14日/30日留存，按 cohort 计算。

## 20.2 Cohort 分析

以用户首次活跃/注册日期为 cohort，生成 cohort_date / day_0 / day_1 /
day_3 / day_7 / day_14 / day_30，前端用热力图展示。

------------------------------------------------------------------------

# 21. 用户生命周期

定义阶段：新用户 / 成长期 / 活跃用户 / 高价值用户 / 沉默用户 / 流失风险

规则必须配置化，例如：

``` text
最近7天有行为 → 活跃
7~30天无行为 → 沉默
30天以上无行为 → 流失风险
```

不要把阈值散落在代码里，统一放入配置文件。

------------------------------------------------------------------------

# 22. RFM

``` text
R = 最近购买距分析日天数
F = 分析周期购买次数
M = 分析周期消费金额
```

评分 1\~5，生成 R_score / F_score / M_score / RFM_score / segment。

分群：高价值 / 重要保持 / 潜力 / 一般 / 沉睡 / 流失风险

------------------------------------------------------------------------

# 23. 用户画像与用户分群

## 23.1 用户画像 user_profile

包括：基础属性、行为统计、购买统计、消费能力、活跃时间、偏好类别、偏好品牌、RFM、生命周期、渠道、设备。

## 23.2 用户分群

除 RFM 外，进阶实现 KMeans。

特征：total_pv / total_click / total_buy / total_amount /
avg_order_amount / purchase_frequency / recency

输出 cluster_id / cluster_name，需要对聚类结果进行业务解释。

------------------------------------------------------------------------

# 24. 用户购买路径分析

分析典型路径，例如：

``` text
PV → Click → Cart → Buy
PV → Click → Collect → Cart → Buy
PV → Search → Click → Buy
```

统计路径数量、用户数量、最终购买率。

> 注意：数据只有页面行为时，不要伪造不存在的页面类型。如果没有 `search`
> 行为，就不要生成"搜索路径"。

------------------------------------------------------------------------

# 25. 商品生命周期

按时间窗口统计商品曝光/点击/加购/购买/GMV，判断新品/成长/爆款/成熟/衰退。建议使用最近
N 个窗口的趋势斜率和销量变化。

------------------------------------------------------------------------

# 26. 商品关联规则

使用 Apriori / FP-Growth。

输入：order_id → item_id 集合

指标：Support / Confidence / Lift（例如：手机 → 手机壳，笔记本 → 鼠标）

规则必须来自真实生成的数据。

------------------------------------------------------------------------

# 27. 价格分析

``` text
价格区间 → 点击率 → 加购率 → 购买率 → GMV
```

价格区间可自动分箱。同时分析价格 vs 购买频率、价格 vs 用户价值、价格 vs
转化率。

------------------------------------------------------------------------

# 28. 渠道与设备分析

## 28.1 渠道分析

渠道：organic / search / ads / campaign / recommendation

分析：用户数、新用户、活跃率、点击率、购买率、GMV、客单价、用户生命周期、用户长期价值。

> 如果没有广告成本字段，不要直接声称是真实 ROI，只能称为"渠道质量对比"。

## 28.2 设备分析

设备：mobile / pc / tablet

比较：活跃、转化、GMV、客单价、使用时间。

------------------------------------------------------------------------

# 29. 特征工程体系

## 用户特征

``` text
age, gender, city, total_pv, total_click, total_collect, total_cart, total_buy,
total_amount, avg_order_amount, recency, frequency, monetary,
favorite_category, favorite_brand, active_days
```

## 商品特征

``` text
price, category, brand, pv, click, collect, cart, buy, gmv,
conversion_rate, unique_users
```

## User-Item 特征

``` text
user_item_pv, user_item_click, user_item_collect, user_item_cart, user_item_buy,
last_interaction, interaction_count, category_preference, brand_preference, price_preference
```

------------------------------------------------------------------------

# 30. 训练集与测试集（防数据泄漏，强制规则）

所有预测模型和推荐评估都必须遵循时间原则：

``` text
前80%时间 → train
后20%时间 → test
```

禁止随机拆分导致未来数据进入训练集。

------------------------------------------------------------------------

# 31. 购买预测模型（P0）

目标：预测用户未来7天是否购买。标签：未来7天发生购买 = 1，否则 = 0。

模型依次：Logistic Regression → Random Forest →（P1）XGBoost/LightGBM

评估必须：Accuracy / Precision / Recall / F1 / ROC-AUC /（可选）PR-AUC /
Confusion Matrix

> 如果类别不平衡：不允许只看 Accuracy，必须重点分析 Precision / Recall /
> F1 / AUC。

------------------------------------------------------------------------

# 32. 用户流失预测（P0）

定义：在观察窗口内活跃，但未来 30 天没有关键行为/购买。

模型：Logistic Regression / Random Forest /（P1）LightGBM

特征：最近行为距今天数、活跃天数、购买次数、最近购买金额、平均购买间隔、浏览次数、加购次数、收藏次数

输出：user_id / churn_probability /
risk_level（low/medium/high），前端展示高风险用户列表。

------------------------------------------------------------------------

# 33. 用户价值预测（P1）

预测未来30天消费金额。可用 Linear Regression / Random Forest Regressor /
LightGBM Regressor。

指标：MAE / RMSE / R²

------------------------------------------------------------------------

# 34. CTR 预测（P1）

目标：用户看到商品后是否点击。

特征：用户特征、商品特征、用户-商品特征、时间特征、渠道、设备

模型：Logistic Regression / LightGBM，输出
click_probability，用于推荐排序。

------------------------------------------------------------------------

# 35. 推荐系统总体设计（P0 核心 + P1 进阶）

推荐系统必须可插拔：

``` text
BaseRecommender
├── PopularRecommender        (P0)
├── ItemCFRecommender         (P0)
├── UserCFRecommender         (P0)
├── ContentBasedRecommender   (P0)
├── HybridRecommender         (P0)
└── TwoStageRecommender       (P1)
```

统一接口：

``` python
recommend(user_id: str, top_k: int = 10)
```

## 35.1 Popular 推荐

使用 PV/Click/Collect/Cart/Buy
综合计算热门度，必须标准化、时间衰减、权重可配置。

## 35.2 Item-CF

用户-商品矩阵权重：pv=1, click=2, collect=3, cart=4, buy=5

计算 item-item cosine similarity，流程：用户历史商品 → 相似商品 →
分数累加 → 过滤 → Top-K

## 35.3 User-CF

构建 user-user similarity，流程：目标用户 → 相似用户 →
相似用户喜欢的商品 → 过滤 → 加权 → Top-K

## 35.4 Content-Based

使用 category / brand / price_range / item tags，转换为向量（One-Hot /
TF-IDF），计算 cosine similarity。

## 35.5 Hybrid 推荐

融合 ItemCF / UserCF / Popular / Content /
UserPreference，统一先归一化到 \[0,1\]：

``` text
HybridScore = w1*ItemCF + w2*UserCF + w3*Popular + w4*Content + w5*Preference
```

所有权重配置化。

## 35.6 冷启动

-   新用户：Popular + 分类热门
-   少行为用户：Popular + ItemCF
-   新商品：Content-Based + 分类热门

## 35.7 推荐过滤

过滤：已购买 / 已推荐 / 下架 / 不存在 /
重复。必要时增加类别/品牌/价格区间多样性（P1）。

## 35.8 推荐解释

每条推荐必须尽量提供
reason，例如"与你最近浏览的商品相似""与你购买过的商品经常一起购买"。**不能对没有证据的推荐生成虚假解释。**

------------------------------------------------------------------------

# 36. 推荐评估（P0）

必须实现：Precision@K / Recall@K / F1@K / HitRate@K / NDCG@K / Coverage
/（P1）Diversity

比较：Popular / ItemCF / UserCF / Content / Hybrid

最终输出表：Algorithm \| Precision@10 \| Recall@10 \| F1@10 \|
HitRate@10 \| NDCG@10 \| Coverage

离线评估原则：历史行为 → train，未来行为 → test；推荐只能使用 train
信息，test 只用于评价。

------------------------------------------------------------------------

# 37. 多路召回与排序模型（P1，进阶）

``` text
User → [ItemCF | Popular | Content] → Candidate Set → Filtering
     → Feature Engine → Ranking(LightGBM) → Top-K
```

排序模型输入：user/item/user-item/context 特征，目标 click 或
buy，输出排序分数。

推荐系统最终架构（P0 到 P1 演进路径）：

``` text
User → Recall(Popular/ItemCF/UserCF/Content) → Candidate Pool → Deduplication
     → Filtering → Feature Engineering →（P1: Ranking Model）→ Diversity(P1) → Top-K → Explanation
```

------------------------------------------------------------------------

# 38. FastAPI 总体接口（P0）

统一前缀 `/api`

``` http
GET /api/health

GET /api/dashboard/overview
GET /api/dashboard/user-trend
GET /api/dashboard/gmv-trend
GET /api/dashboard/behavior-trend
GET /api/dashboard/funnel
GET /api/dashboard/retention

GET /api/users
GET /api/users/{user_id}
GET /api/users/{user_id}/profile
GET /api/users/{user_id}/behaviors
GET /api/users/{user_id}/orders
GET /api/users/{user_id}/recommendations

GET /api/items
GET /api/items/{item_id}
GET /api/items/ranking
GET /api/items/{item_id}/statistics

GET /api/analysis/rfm
GET /api/analysis/lifecycle
GET /api/analysis/cohort
GET /api/analysis/path
GET /api/analysis/channel
GET /api/analysis/price
GET /api/analysis/association

GET /api/models/purchase
GET /api/models/churn
GET /api/models/metrics

GET /api/recommendations/{user_id}
GET /api/recommendations/{user_id}/compare
GET /api/recommendations/metrics
```

## API 分层（强制）

``` text
Router → Service → Repository → Database
```

算法链路：

``` text
Router → RecommendationService → Recommender → Model / Cache / Repository
```

禁止把业务逻辑全部写在 Router。

------------------------------------------------------------------------

# 39. 前端页面（P0）

至少：`/dashboard` `/users` `/users/:id` `/items` `/items/:id`
`/analysis` `/recommendations` `/models`

## 39.1 Dashboard

KPI：用户数、活跃用户、购买用户、订单数、GMV、客单价

图表：DAU趋势、GMV趋势、用户行为趋势、商品TOP10、分类TOP10、漏斗、留存热力图、渠道表现

## 39.2 用户分析页面

用户数量、活跃趋势、城市分布、RFM分群、生命周期、用户价值、高风险流失用户

## 39.3 用户详情页

基础信息、行为统计、购买统计、RFM、生命周期、偏好分类、偏好品牌、最近行为、推荐商品、购买预测、流失概率

## 39.4 商品分析页面

商品排行、分类排行、品牌排行、价格分布、转化率、生命周期、关联商品

## 39.5 推荐页面

允许输入 user_id，可选算法
Popular/ItemCF/UserCF/Content/Hybrid/（P1）TwoStage，展示推荐商品、分数、排名、推荐原因，并支持算法对比。

## 39.6 模型页面

购买预测（AUC/Precision/Recall/F1）、流失预测（同上）、推荐（Precision@10/Recall@10/NDCG@10/HitRate@10/Coverage）

## 39.7 ECharts

至少 Line / Bar / Pie / Funnel / Heatmap / Scatter
/（可选）Radar，所有图表必须来自 API，禁止用写死数组作为最终展示数据。

------------------------------------------------------------------------

# 40. Redis（P1）

用途：Dashboard KPI、热门商品、用户画像、推荐结果、模型预测缓存

Key 示例：`dashboard:overview` `ranking:items:hot` `user:profile:U001`
`recommend:user:U001` `prediction:churn:U001`，设置合理 TTL。

------------------------------------------------------------------------

# 41. Kafka / Spark / Flink（P2，仅设计，不实现）

**这一节的输出物是文档，不是代码。**

在 `docs/architecture.md` 中说明：

-   实时链路设计：`Frontend/Simulator → Kafka(topic: user_behavior, order_event) → Consumer → Redis → FastAPI → Dashboard`，实时指标包括实时PV/实时活跃/实时热门商品/实时订单/实时GMV。
-   大数据演进设计：当行为数据达到千万级后，ETL/画像/离线特征/批量推荐可迁移至
    Spark；实时行为分析/实时窗口统计/实时特征可采用 Flink。
-   明确写出"当前数据规模下为什么不实现"，以及"如果数据量增长到什么阈值会考虑引入"，这是面试第40题的标准答题素材。

------------------------------------------------------------------------

# 42. Docker（P0，仅 backend/frontend/mysql；Redis 为 P1 追加）

``` text
backend
frontend
mysql
```

（P1 追加：redis）

启动：`docker compose up -d`

------------------------------------------------------------------------

# 43. 配置管理

使用 `.env`：

``` env
APP_ENV=development

MYSQL_HOST=127.0.0.1
MYSQL_PORT=3306
MYSQL_DATABASE=ecommerce_recommendation
MYSQL_USER=root
MYSQL_PASSWORD=

REDIS_HOST=127.0.0.1
REDIS_PORT=6379
REDIS_PASSWORD=

RECOMMEND_TOP_K=10
```

禁止把密码写入代码。

------------------------------------------------------------------------

# 44. 日志

记录：API request / ETL / data quality / model training / model loading
/ recommendation / latency / errors

示例：`INFO recommendation user=U001 strategy=hybrid latency=12ms`

------------------------------------------------------------------------

# 45. 测试

-   **数据**：数据量、空值、重复、外键一致性
-   **分析**：DAU、GMV、漏斗、RFM、留存
-   **模型**：训练成功、预测输出、指标计算
-   **推荐**：Top-K、过滤、冷启动、ItemCF、UserCF、Hybrid
-   **API**：Health、Dashboard、User、Item、Recommendation

------------------------------------------------------------------------

# 46. 性能要求

## 数据库索引

建立 user_id / item_id / event_time / event_date / behavior_type /
category_id / order_id 索引。

## Pandas

大文件使用 `pd.read_csv(..., chunksize=...)`

## 推荐

禁止"HTTP请求 → 重新训练推荐模型"。应为：离线训练 → 保存模型/相似度 →
API加载。

------------------------------------------------------------------------

# 47. 模型文件与版本

使用 joblib 保存：`purchase_model.joblib` `churn_model.joblib`
`item_similarity.joblib` `user_similarity.joblib`（P1:
`ctr_model.joblib`）

同时保存 `feature_columns.json` `model_metadata.json`

版本目录建议：

``` text
models/
├── purchase/{v1,v2}/
├── churn/
└── recommendation/
```

metadata 示例：

``` json
{
  "model_name": "purchase_prediction",
  "version": "v1",
  "train_time": "2026-08-12",
  "features": 25,
  "metrics": { "f1": 0.71, "auc": 0.82 }
}
```

## 可复现性

必须记录随机种子、数据时间范围、特征版本、模型参数、训练时间、评估指标。统一使用
`RANDOM_STATE = 42`。

------------------------------------------------------------------------

# 48. 项目运行方式

``` bash
# 1. 创建环境
python -m venv .venv

# 2. 激活环境（Windows）
.venv\Scripts\activate
# Linux/macOS: source .venv/bin/activate

# 3. 安装依赖
pip install -r backend/requirements.txt
pip install -r analysis/requirements.txt

# 4. 初始化数据库
python scripts/init_db.py

# 5. 生成数据
python scripts/generate_data.py

# 6. ETL
python scripts/run_etl.py

# 7. 运行分析
python scripts/run_all.py

# 8. 启动后端
uvicorn backend.app.main:app --reload

# 9. 启动前端
cd frontend
npm install
npm run dev
```

------------------------------------------------------------------------

# 49. 全流程 Phase 设计与当前执行计划

> **重要：Phase 0\~3 已完成。**
>
> 从现在开始，代码生成 LLM 的工作起点是 **Phase 4**。
>
> 工期只是参考，不是硬性要求。优先保证正确性、可运行性和可解释性。

## 49.1 当前进度

  Phase   状态        主要内容
  ------- ----------- ------------------------------------------------
  0       ✅ 已完成   需求、架构、目录、README
  1       ✅ 已完成   FastAPI、config、logging、database、health API
  2       ✅ 已完成   MySQL 核心表、约束、索引
  3       ✅ 已完成   业务规律模拟数据生成器
  4       ▶️ 下一步   数据质量 + ETL
  5       ⏳          基础分析
  6       ⏳          留存 + Cohort + RFM
  7       ⏳          深度业务分析
  8       ⏳          特征工程
  9       ⏳          购买预测
  10      ⏳          流失预测
  11      ⏳          Popular Baseline
  12      ⏳          Item-CF + User-CF
  13      ⏳          Content-Based
  14      ⏳          Hybrid
  15      ⏳          推荐评估
  16      ⏳          FastAPI 全量接口
  17      ⏳          Vue3 全量页面
  18      ⏳          测试 + 性能优化
  19      ⏳          最终文档 + Demo + 面试材料

## 49.2 Phase 4：数据质量 + ETL【下一阶段】

必须完成：

``` text
Phase 3 Raw Data
      ↓
Schema Validation
      ↓
Cleaning
      ↓
Quality Report
      ↓
Transformation
      ↓
Processed Data
      ↓
MySQL
```

验收：

-   原始数据可以被读取。
-   数据质量报告可以生成。
-   空值、重复、非法值可以被识别。
-   外键逻辑可以检查。
-   时间逻辑可以检查。
-   ETL 可以重复运行。
-   重复运行不会产生重复订单/行为。
-   大文件使用批量读取和批量写入。
-   生成 `dataset_version` 和 ETL 运行记录。

**Phase 4 不实现机器学习和推荐。**

## 49.3 Phase 5：基础分析

完成：

``` text
用户规模
DAU / WAU / MAU
行为分析
活跃时间
GMV
订单
客单价
ARPU
商品排行
分类排行
漏斗
```

要求：

-   分析结果保存为结构化数据。
-   后续 API 可以直接复用。
-   指标计算逻辑必须有测试。

## 49.4 Phase 6：留存 + Cohort + RFM

完成：

``` text
Retention
Cohort
RFM
```

验收：

-   留存口径明确。
-   cohort 起点明确。
-   R/F/M 定义明确。
-   RFM 分数规则可配置。
-   用户分群结果可解释。

## 49.5 Phase 7：深度业务分析

完成：

``` text
生命周期
购买路径
商品生命周期
价格
渠道
设备
关联规则
用户分群
用户画像
商品画像
```

业务发现输出必须升级为：

``` text
现象
 ↓
证据
 ↓
可能原因
 ↓
业务建议
```

同时注明：

> 由于数据为模拟数据，原因和建议是方法论演示，不得表述为真实电商业务结论。

## 49.6 Phase 8：特征工程

必须明确：

``` text
Observation Window
```

例如：

``` text
过去30天行为
       ↓
生成用户/商品/交互特征
```

同时生成：

``` text
feature_version
feature_time_range
```

要求：

-   特征只允许使用观察窗口内的数据。
-   不得读取未来标签信息。
-   特征计算过程可复现。
-   特征字段有数据字典。

## 49.7 Phase 9：购买预测

目标：

``` text
过去30天行为
       ↓
特征
       ↓
未来7天是否购买
       ↓
label
```

模型：

``` text
Logistic Regression
Random Forest
```

评估：

``` text
Precision
Recall
F1
ROC-AUC
PR-AUC（建议）
Confusion Matrix
```

要求：

-   时间切分。
-   防止数据泄漏。
-   类别不平衡时不能只看 Accuracy。
-   保存模型和 metadata。
-   输出特征重要性/系数解释。

## 49.8 Phase 10：流失预测

目标：

``` text
过去30天行为
       ↓
特征
       ↓
未来30天无关键行为/购买
       ↓
churn label
```

输出：

``` text
user_id
churn_probability
risk_level
```

必须说明：

``` text
观察窗口
预测窗口
流失定义
```

## 49.9 Phase 11：Popular Baseline

推荐系统必须从 Baseline 开始。

热门分数建议：

``` text
score =
w_pv * pv_score
+ w_click * click_score
+ w_collect * collect_score
+ w_cart * cart_score
+ w_buy * buy_score
```

同时加入：

``` text
time_decay
```

权重必须配置化。

## 49.10 Phase 12：Item-CF + User-CF

行为权重不能硬编码在业务逻辑中。

例如：

``` yaml
behavior_weights:
  pv: 1
  click: 2
  collect: 3
  cart: 4
  buy: 5
```

至少做一次权重实验：

``` text
实验A：1/2/3/4/5
实验B：1/2/4/6/8
实验C：1/2/3/5/10
```

比较：

``` text
Precision@10
Recall@10
NDCG@10
HitRate@10
Coverage
```

最终选择依据必须来自离线实验，而不是"感觉这个权重更合理"。

## 49.11 Phase 13：Content-Based

使用：

``` text
category
brand
price_range
item tags
```

构造商品向量并计算相似度。

必须解决：

-   已购买商品过滤。
-   已下架商品过滤。
-   Top-K。
-   冷启动商品。

## 49.12 Phase 14：Hybrid

统一：

``` text
Popular
ItemCF
UserCF
Content
```

分数归一化后融合：

``` text
HybridScore =
w1 * ItemCF
+ w2 * UserCF
+ w3 * Popular
+ w4 * Content
```

要求：

-   权重可配置。
-   至少做 baseline vs hybrid 对比。
-   最终结论必须基于评估指标。

## 49.13 Phase 15：推荐评估

必须严格使用时间切分：

``` text
历史行为 → train
未来行为 → test
```

比较：

``` text
Popular
ItemCF
UserCF
Content
Hybrid
```

输出：

``` text
Algorithm
Precision@10
Recall@10
F1@10
HitRate@10
NDCG@10
Coverage
```

如果 Hybrid 没有优于 Popular，不允许强行声称 Hybrid 更好。

## 49.14 Phase 16：FastAPI 全量接口

只有分析、模型、推荐逻辑稳定后才进入全量 API。

分层：

``` text
Router
 ↓
Service
 ↓
Repository / Algorithm
 ↓
Database / Model
```

禁止：

``` text
Router
 └── 直接塞大量业务逻辑
```

## 49.15 Phase 17：Vue3 全量页面

核心页面：

``` text
Dashboard
Users
User Detail
Items
Item Detail
Analysis
Recommendations
Models
```

最终 Demo 必须能够：

``` text
选择用户
 ↓
查看用户画像
 ↓
查看RFM/生命周期
 ↓
查看购买概率/流失概率
 ↓
获取推荐
 ↓
切换推荐算法
 ↓
查看推荐原因
 ↓
查看推荐评估
```

## 49.16 Phase 18：测试 + 性能优化

至少：

``` text
数据质量测试
分析指标测试
模型训练测试
推荐 Top-K 测试
推荐过滤测试
冷启动测试
API 测试
数据库索引检查
```

重点不是追求极高覆盖率，而是保证核心链路不被修改破坏。

## 49.17 Phase 19：最终文档 + Demo + 秋招材料

必须完成：

``` text
README.md
architecture.md
database.md
analysis.md
machine_learning.md
recommendation.md
api.md
deployment.md
interview.md
```

另外新增：

``` text
demo.md
experiment.md
```

`experiment.md` 至少记录：

-   推荐算法对比
-   行为权重实验
-   Hybrid 权重实验
-   购买预测模型对比
-   流失预测模型对比
-   最终选择理由

## 49.18 P1

P0 完成后再考虑：

``` text
Redis
用户价值预测
CTR预测
多路召回
LightGBM排序
推荐多样性
```

## 49.19 P2

只设计：

``` text
Kafka
Spark
Flink
```

禁止为了"技术栈好看"编写没有实际业务价值的伪实时系统。

------------------------------------------------------------------------

------------------------------------------------------------------------

# 50. 业务发现模块（V2.2）

分析代码不能只输出数字。

每个重要发现至少包含：

``` text
metric
evidence
phenomenon
possible_reason
business_action
```

例如：

``` text
指标：
18~23点行为量占比

现象：
晚间是主要活跃时段。

证据：
晚间行为量显著高于其他时段。

可能原因：
模拟数据在生成阶段设置了晚间活跃规律。

业务建议：
如果是真实业务，可进一步评估晚间推荐位和活动资源配置。
```

### 50.1 严格禁止

禁止输出：

``` text
“真实用户喜欢晚上购物”
```

因为当前数据是模拟数据。

应该输出：

``` text
“在当前模拟数据规则下，晚间行为量更高。”
```

### 50.2 业务发现的作用

业务发现不是为了"让报告看起来像商业分析"，而是为了证明：

``` text
数据
 ↓
指标
 ↓
分析
 ↓
解释
 ↓
决策
```

这是项目从"算法 Demo"升级成"数据应用"的关键。

------------------------------------------------------------------------

------------------------------------------------------------------------

# 51. 项目最终验收清单

## 当前已完成

-   [x] Phase 0：项目架构与基础目录
-   [x] Phase 1：FastAPI 基础能力
-   [x] Phase 2：MySQL 核心数据表
-   [x] Phase 3：业务规律模拟数据生成器

## 数据（P0）

-   [ ] 数据质量检查
-   [ ] ETL
-   [ ] MySQL 数据导入闭环
-   [ ] 数据量可配置
-   [ ] dataset_version
-   [ ] ETL 可重复执行
-   [ ] 事实层 / 维度层 / 派生层边界明确

## 基础分析（P0）

-   [ ] DAU/WAU/MAU
-   [ ] GMV
-   [ ] 客单价
-   [ ] ARPU
-   [ ] 漏斗
-   [ ] 商品排行
-   [ ] 分类排行
-   [ ] 活跃时间

## 深度分析（P0）

-   [ ] 留存
-   [ ] Cohort
-   [ ] RFM
-   [ ] 生命周期
-   [ ] 路径
-   [ ] 关联规则
-   [ ] 价格
-   [ ] 渠道
-   [ ] 设备
-   [ ] 用户画像
-   [ ] 商品画像
-   [ ] 业务发现（现象 → 证据 → 原因 → 建议）

## 机器学习（P0）

-   [ ] 用户特征
-   [ ] 商品特征
-   [ ] User-Item 特征
-   [ ] Observation Window
-   [ ] Prediction Window
-   [ ] 购买预测
-   [ ] 流失预测
-   [ ] 时间切分
-   [ ] 数据泄漏检查
-   [ ] 模型版本
-   [ ] 模型 metadata

## 推荐（P0）

-   [ ] Popular
-   [ ] ItemCF
-   [ ] UserCF
-   [ ] Content
-   [ ] Hybrid
-   [ ] 冷启动
-   [ ] 推荐解释
-   [ ] 已购买商品过滤
-   [ ] 下架商品过滤
-   [ ] Precision@K
-   [ ] Recall@K
-   [ ] HitRate@K
-   [ ] NDCG@K
-   [ ] Coverage
-   [ ] Popular Baseline 对比
-   [ ] 行为权重实验
-   [ ] Hybrid 权重实验

## 工程（P0）

-   [ ] FastAPI
-   [ ] Vue3
-   [ ] ECharts
-   [ ] pytest
-   [ ] logging
-   [ ] 配置管理
-   [ ] Docker
-   [ ] API 分层
-   [ ] 数据库索引
-   [ ] 最终 Demo 闭环

## P1

-   [ ] Redis
-   [ ] 用户价值预测
-   [ ] CTR预测
-   [ ] 多路召回
-   [ ] LightGBM排序
-   [ ] 推荐多样性

## P2

-   [ ] Kafka 架构设计
-   [ ] Spark 架构设计
-   [ ] Flink 架构设计
-   [ ] 数据扩大100倍的演进方案

------------------------------------------------------------------------

------------------------------------------------------------------------

# 52. 秋招简历应该突出什么

不要把所有技术栈全部堆在简历上，建议最终形成三类亮点：

## 数据分析方向

``` text
基于 Python 构建电商用户行为分析平台，完成 DAU/GMV/漏斗/Cohort/RFM/生命周期等分析，
并通过用户路径、渠道及商品关联规则挖掘业务规律。
```

## 数据科学/算法方向

``` text
构建购买预测与流失预测模型，使用时间切分避免数据泄漏，
并实现 Popular、User-CF、Item-CF、Content-Based 及 Hybrid 多策略推荐。
```

## 数据开发方向

``` text
搭建 Python ETL 数据处理链路，使用 MySQL 完成数据存储，
通过 FastAPI 提供分析与推荐服务，并给出 Redis/Kafka 的缓存与实时数据处理扩展方案。
```

> 注意最后一条措辞是"给出扩展方案"而不是"已实现"，与 P2
> 的实际完成状态保持一致，避免简历与实际项目不符。

------------------------------------------------------------------------

# 53. 项目面试必须能讲清楚的问题

## A. 当前已经完成的 Phase 0\~3

1.  为什么选择这个项目？
2.  项目整体架构是什么？
3.  为什么 Phase 0\~3 这样拆？
4.  MySQL 六张核心表分别是什么？
5.  为什么要区分用户、商品、行为、订单、订单明细？
6.  模拟数据为什么不能完全随机？
7.  你的数据生成器加入了哪些业务规律？
8.  如何控制数据量？
9.  如果重新生成数据，如何保证版本可追踪？

## B. Phase 4\~7：数据与分析

10. ETL 为什么要分 Raw / Processed？
11. 如何保证 ETL 可重复执行？
12. 如何检查数据质量？
13. DAU/WAU/MAU 怎么定义？
14. GMV、客单价、ARPU 怎么计算？
15. 漏斗的每一步转化率怎么计算？
16. 留存和 Cohort 的区别是什么？
17. RFM 为什么有效？
18. 生命周期如何定义？
19. 购买路径如何构造？
20. 关联规则中的 Support / Confidence / Lift 是什么？
21. 模拟数据下的业务发现有什么局限？

## C. Phase 8\~10：机器学习

22. 为什么购买预测需要 Observation Window + Prediction Window？
23. 如何防止未来信息泄漏？
24. 为什么使用时间切分而不是随机切分？
25. 类别不平衡怎么办？
26. 为什么不能只看 Accuracy？
27. AUC / F1 / Precision / Recall 分别适合什么情况？
28. 流失标签怎么定义？
29. 哪些特征可能最重要？为什么？
30. 如何保存和管理模型版本？

## D. Phase 11\~15：推荐系统

31. 为什么先做 Popular Baseline？
32. Item-CF 和 User-CF 的区别是什么？
33. 为什么使用 Cosine Similarity？
34. PV / Click / Collect / Cart / Buy 为什么需要不同权重？
35. 行为权重如何通过实验确定？
36. 如何解决新用户冷启动？
37. 如何解决新商品冷启动？
38. 为什么要过滤已经购买商品？
39. Hybrid 为什么可能优于单一算法？
40. Precision@K / Recall@K / HitRate@K / NDCG@K / Coverage 分别是什么？
41. 如果 Hybrid 没有超过 Popular，你怎么办？
42. 推荐接口为什么不能每次请求都重新训练？

## E. Phase 16\~19：工程化

43. 为什么使用 Router → Service → Repository？
44. 为什么前端不能直接连接数据库？
45. 推荐模型如何从离线训练变成在线服务？
46. 为什么需要缓存？
47. 哪些结果适合缓存？
48. 如何处理模型加载和模型版本？
49. 如何测试推荐系统？
50. 如果数据扩大100倍怎么办？
51. 为什么当前不实现 Kafka/Spark/Flink？
52. 如果未来要加入 Kafka/Flink，数据链路怎么演进？

## F. 最终 Demo 必须能现场讲

``` text
打开 Dashboard
    ↓
查看总体业务指标
    ↓
进入某个用户
    ↓
查看用户画像 / RFM / 生命周期
    ↓
查看购买概率 / 流失概率
    ↓
进入推荐页面
    ↓
选择 Popular / ItemCF / UserCF / Content / Hybrid
    ↓
查看 Top-K 和推荐原因
    ↓
打开推荐评估
    ↓
解释为什么最终选择某种策略
```

面试讲解的核心不是：

> "我用了很多技术。"

而是：

> "我如何从数据出发，一步一步形成用户理解、预测和推荐，并最终把结果做成一个可以调用和展示的系统。"

------------------------------------------------------------------------

------------------------------------------------------------------------

# 54. 代码生成 LLM 的强制规则

## 54.1 当前最重要的规则：从 Phase 4 继续

1.  **Phase 0\~3 已完成，不得重新实现。**
2.  每次接到任务后先判断它属于哪个 Phase。
3.  如果任务属于 Phase 4 以前，默认已有实现；只有发现真实 Bug 才修改。
4.  不得为了"代码更漂亮"重写已有模块。
5.  如果必须修改已有代码，必须先说明：
    -   修改原因
    -   影响范围
    -   是否兼容已有数据
    -   是否需要迁移
    -   如何回滚

## 54.2 通用规则

6.  **禁止一次性生成整个项目**：每次只完成一个明确阶段/子任务。
7.  **先检查现状**：读取当前代码、schema、配置和相关测试后再修改。
8.  **每阶段必须运行**：告诉用户运行命令、预期结果和验证方法。
9.  **不允许伪实现**：禁止 `pass` / `return mock_data` / `return []`
    作为核心业务实现。
10. **禁止前端写死分析数据**：最终数据必须来自 API。
11. **算法必须真实**：禁止随机数冒充模型效果。
12. **防止数据泄漏**：预测和推荐评估必须严格遵守时间切分。
13. **核心代码必须可解释**：说明输入、输出、算法、复杂度、业务意义。
14. **结果必须可复现**：统一记录随机种子、数据版本、特征版本、模型版本、参数和指标。
15. **P0 完成前不得开始 P1。**
16. **P2 只产出设计文档**，禁止编写 Kafka/Spark/Flink 的伪实现。
17. **禁止无理由新增依赖**。
18. **禁止为了炫技引入组件**。
19. **优先复用已有代码**，不要重复造轮子。
20. **核心指标必须有测试或可人工核验方法。**

## 54.3 数据规则

21. 原始事实数据和派生数据必须分开。
22. ETL 必须可重复执行。
23. 大文件必须采用批量读取/写入。
24. 所有除法必须处理除零。
25. 时间相关指标必须明确统计窗口。
26. 模型特征必须明确 Observation Window。
27. 模型标签必须明确 Prediction Window。
28. 禁止使用 Prediction Window 中的信息生成特征。

## 54.4 推荐规则

29. 所有推荐算法实现统一接口。
30. Popular 必须作为 Baseline。
31. 推荐必须过滤：

-   已购买商品
-   下架商品
-   不存在商品
-   重复商品

32. 行为权重必须配置化。
33. Hybrid 权重必须允许实验。
34. 推荐效果必须使用未来行为作为测试依据。
35. 如果实验结果不支持某算法更优，必须如实保留结果。

------------------------------------------------------------------------

------------------------------------------------------------------------

# 55. 开发过程中 LLM 的输出格式

每完成一个 Phase，必须输出：

1.  本阶段目标
2.  **当前仓库状态检查结果**
3.  修改/新增文件
4.  修改了哪些已有文件，以及为什么
5.  完整代码
6.  运行命令
7.  验证方法
8.  测试结果
9.  常见错误
10. 当前已知问题
11. 下一阶段

特别要求：

> 如果当前阶段依赖 Phase 0\~3
> 已完成的代码，必须明确说明"复用了哪些现有模块"，不能把已有功能重新实现一遍。

不要在没有验证上一阶段的情况下直接跳到下一阶段。

------------------------------------------------------------------------

------------------------------------------------------------------------

# 56. 最终能力矩阵

``` text
Python
├── 数据处理: Pandas / NumPy / SQL / ETL / 数据质量
├── 数据分析: DAU/MAU / GMV / Funnel / Retention / Cohort / RFM / Lifecycle / Path / Association / Channel
├── Machine Learning: Purchase Prediction / Churn Prediction /（P1）Value Prediction / CTR Prediction
├── Recommendation: Popular / ItemCF / UserCF / Content / Hybrid /（P1）Recall / Ranking
├── Backend: FastAPI / SQLAlchemy / RESTful API
├── Storage: MySQL /（P1）Redis
├── Streaming（P2，仅设计）: Kafka / Spark / Flink
├── Visualization: Vue3 / ECharts
└── Engineering: Git / pytest / Docker / Logging
```

------------------------------------------------------------------------

# 57. 项目最终闭环

``` text
数据 → 数据质量 → ETL → 数据仓库
  ↓
用户分析 / 商品分析 / 渠道分析
  ↓
深度分析（RFM / 生命周期 / 路径 / 关联规则 / 价格 / 设备）
  ↓
特征工程
  ↓
预测模型（购买/流失，P1:价值/CTR） ｜ 推荐系统（Popular/CF/Content/Hybrid，P1:召回/排序）
  ↓
模型评估
  ↓
FastAPI → MySQL /（P1）Redis
  ↓
Vue3 + ECharts
  ↓
可视化平台 → 业务决策/推荐
```

------------------------------------------------------------------------

# 58. 最终项目 Demo 与项目主线

## 58.1 项目主线

最终不要把项目讲成"很多功能的集合"，而要讲成：

``` text
用户行为数据
      ↓
数据清洗 / ETL
      ↓
数据分析
      ↓
用户画像
      ↓
购买 / 流失预测
      ↓
推荐系统
      ↓
推荐效果评估
      ↓
FastAPI
      ↓
Vue3 Dashboard
```

## 58.2 四层问题

### 第一层：发生了什么？

``` text
DAU
GMV
漏斗
商品
分类
渠道
```

### 第二层：为什么？

``` text
RFM
Cohort
生命周期
路径
关联规则
价格
```

### 第三层：接下来会怎样？

``` text
购买预测
流失预测
```

### 第四层：应该做什么？

``` text
Popular
ItemCF
UserCF
Content
Hybrid
```

## 58.3 最终 Demo

面试现场建议按以下顺序：

``` text
1. Dashboard
   ↓
2. 用户分析
   ↓
3. 用户详情
   ↓
4. 用户画像 / RFM / 生命周期
   ↓
5. 购买概率 / 流失概率
   ↓
6. 推荐页面
   ↓
7. 切换算法
   ↓
8. 查看推荐原因
   ↓
9. 查看推荐指标
   ↓
10. 解释最终算法选择
```

最终要能够在 5\~10 分钟内完成一次完整演示。

## 58.4 最终项目的一句话定位

推荐使用：

> **基于 Python
> 的电商用户行为分析与智能推荐平台：从用户行为数据出发，完成
> ETL、用户画像、业务分析、购买/流失预测和多策略推荐，并通过 FastAPI +
> Vue3 实现可视化与在线服务。**

如果 P1 没有实现，不要在简历中写成已实现。

如果 P2 只有设计，也只能写：

> **设计 Kafka / Spark / Flink 的未来数据链路扩展方案。**

------------------------------------------------------------------------

# 59. 最终原则

这个项目的核心不是"用了多少框架"，而是：

> **能不能从一批原始数据出发，经过数据处理、分析、建模和推荐，最终形成一个真实可运行、可解释、可评估、可展示的数据应用------并且诚实地说明哪些部分是核心实现、哪些部分是仅作架构设计的扩展方向。**

最终应做到：

``` text
数据真实 + 指标正确 + 分析有业务意义 + 模型有评估 + 推荐有对比
+ 接口可调用 + 前端可展示 + 工程可运行 + 代码可解释 + 项目可面试
+ 范围诚实（P0/P1/P2 边界清晰，不夸大未实现部分）
```

此外，V2.2 的一个重要完成标准是：

> **项目必须在已有 Phase 0\~3
> 实现之上连续演进，而不是反复重做前期基础设施。**

当前开发起点是 **Phase 4**。后续每一次修改都必须优先保护已经完成的 Phase
0\~3。

这才是本项目的最终完成标准。
