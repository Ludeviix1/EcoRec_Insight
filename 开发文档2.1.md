# 电商用户行为分析与智能推荐平台

## 全项目全流程开发规格说明书（V2.1 优化版）

> **用途**：本文件专门交给代码生成 LLM，作为项目的总体开发规格、功能边界、技术路线、数据模型、算法要求、API 设计、前端设计、测试标准和迭代路线。
>
> **项目定位**：面向秋招求职展示的综合型 Python 数据分析、机器学习、推荐系统与数据应用平台。
>
> **核心原则**：项目不是简单的"Pandas 数据分析作业"，也不是单纯 CRUD。最终需要形成完整的数据闭环：
>
> **数据生成/采集 → 数据清洗 → ETL → 数据存储 → 数据质量 → 基础分析 → 深度业务分析 → 用户画像 → 商品画像 → 用户分群 → 预测建模 → 推荐系统 → 推荐评估 → FastAPI → Vue3/ECharts → 测试 → 部署 → 项目文档 → 秋招面试材料**
>
> 本文档按照"先完成稳定 MVP，再逐层增强"的方式设计。代码生成 LLM **禁止一次性生成整个项目**，必须严格按照 Phase 顺序开发、运行、测试和修复。

------------------------------------------------------------------------

## 相比 V2.0 的主要变更说明

V2.0 把 Level 1~4 全部列为"必须完成"，且进阶项（多路召回、CTR、LTR、Spark、Flink）与 Kafka 并列在"必须/进阶"之间，导致两个实际问题：

1. **工作量与个人开发时间线不匹配**：全部实现相当于数周到数月的团队级工作量。
2. **规则自相矛盾**：V2.0 规则 8 写着"不为了炫技增加组件"，但同时把 Kafka/Spark/Flink 列为 Level 4 进阶必须项；而这些组件在文档自己规划的数据量（最多 200 万行为记录）下并无实际价值（Spark 价值要到千万级数据才体现）。

**V2.1 的修订方式**：引入 **P0 / P1 / P2 三级优先级**，替代原来"必须/进阶"的二分：

- **P0（核心，必须实现）**：构成一个完整可运行、可展示、可讲清楚的项目所需的最小闭环。对应原 Level 1、Level 2、Level 3 的非进阶部分、Level 4 的非进阶部分。
- **P1（加分项，时间允许再做）**：能显著提升项目深度、但没有也不影响项目成立的部分。对应原 Level 3 进阶（CTR、LTR、多路召回、用户价值预测）和 Redis。
- **P2（仅作架构设计与面试话术，不要求编码实现）**：Kafka、Spark、Flink。这类组件在当前数据规模下投入产出比极低，V2.1 明确**不纳入实现范围**，仅要求在 `docs/architecture.md` 中以"未来扩展方向"形式给出设计方案，用于面试时展示工程视野。这样既保留了简历上的"了解实时链路设计"亮点，又避免了不必要的实现成本，也消除了原规则 8 的自相矛盾。

其余章节结构与内容基本沿用 V2.0，本文档在每个 Phase 标注了优先级和粗略工期估算，供实际排期参考。

------------------------------------------------------------------------

# 0. 项目最终目标

最终项目应该能够回答四类业务问题：

## 0.1 发生了什么？

-   用户有多少？用户什么时候活跃？用户做了什么行为？
-   哪些商品卖得最好？哪些分类表现最好？哪些渠道带来的用户最好？
-   GMV 如何变化？转化漏斗在哪一步流失最严重？

## 0.2 为什么发生？

-   为什么某类用户更容易购买？哪些行为与购买强相关？
-   用户通常经过什么路径完成购买？哪些商品经常一起购买？
-   什么价格区间转化率最高？哪些渠道用户质量最高？
-   哪些用户正在沉默或流失？哪些商品正在进入衰退期？

## 0.3 接下来会发生什么？

-   用户未来 7 天是否可能购买？用户未来 30 天是否可能流失？
-   某用户对某商品的点击概率是多少？哪些用户具有高价值潜力？

## 0.4 应该做什么？

-   应该向用户推荐什么商品？为什么推荐这个商品？
-   不同推荐算法哪个更好？推荐系统覆盖了多少商品？
-   如何针对不同用户群采用不同推荐策略？

------------------------------------------------------------------------

# 1. 项目范围与优先级分级

## P0（核心，必须完成 —— 决定项目是否成立）

-   数据生成、数据清洗、ETL、数据质量检查
-   用户分析、商品分析、GMV 分析、漏斗分析、留存分析、RFM
-   用户生命周期、用户购买路径、Cohort 分析、商品生命周期、商品关联规则、价格分析、渠道分析、用户分群、用户画像、商品画像
-   购买预测、流失预测
-   热门推荐、Item-CF、User-CF、内容推荐、Hybrid、冷启动、推荐效果评估
-   FastAPI、Vue3、ECharts、API 分层、日志、测试、配置管理、Docker（backend/frontend/mysql 三容器）

## P1（加分项，P0 完成且时间允许再做）

-   用户价值预测（回归）
-   CTR 预测
-   多路召回、Learning-to-Rank / LightGBM 排序
-   推荐多样性
-   Redis 缓存（Dashboard、推荐结果、用户画像）

## P2（仅设计与面试话术，不要求实现代码）

-   Kafka 实时链路
-   Spark / Flink 大数据处理

> 判断标准：当前数据规模（用户万级、行为百万级）下，Kafka/Spark/Flink 带来的实际收益低于开发和运维成本。这类内容只需要在 `docs/architecture.md` 中给出**架构设计图 + 技术选型理由 + 如果数据量增长 100 倍该如何演进**，作为面试第 40 题（"如果数据扩大100倍怎么办？"）的标准答案素材，不需要真实跑起来。

------------------------------------------------------------------------

# 2. 系统总体架构（P0+P1，落地实现范围）

``` text
                         ┌──────────────────────┐
                         │   用户行为/订单数据   │
                         └──────────┬───────────┘
                                    │
                         数据生成 / 数据采集
                                    ▼
                         ┌──────────────────────┐
                         │       Raw Data       │
                         └──────────┬───────────┘
                                    │
                              数据质量检查
                                    ▼
                         ┌──────────────────────┐
                         │ Cleaning / ETL       │
                         └──────────┬───────────┘
                                    ▼
                              MySQL 数据仓库
                                    ▼
                         ┌──────────────────────┐
                         │    数据分析层        │
                         │ 用户/商品/GMV/漏斗/  │
                         │ 留存/Cohort/RFM/路径 │
                         │ /生命周期/关联/渠道  │
                         └──────────┬───────────┘
                                    ▼
                         ┌──────────────────────┐
                         │      特征工程层       │
                         └──────────┬───────────┘
                   ┌────────────────┴────────────────┐
                   ▼                                 ▼
          ┌──────────────────┐             ┌──────────────────┐
          │ 预测模型(购买/   │             │ 推荐系统         │
          │ 流失，P1:CTR/价值)│             │ Popular/ItemCF/  │
          └────────┬─────────┘             │ UserCF/Content/  │
                   │                       │ Hybrid(P1:多路召回/排序)│
                   │                       └────────┬─────────┘
                   └────────────────┬──────────────┘
                                    ▼
                              模型评估层
                                    ▼
                              FastAPI Service
                          ┌─────────┴─────────┐
                          ▼                   ▼
                       MySQL          Redis(P1，可选)
                          └─────────┬─────────┘
                                    ▼
                              Vue3 + ECharts
                                    ▼
                              数据分析平台
```

## 2.1 扩展架构（P2，仅设计不实现）

``` text
Frontend / Simulator → Kafka → Consumer → Redis → FastAPI → Dashboard
```

用于支撑实时 PV、实时活跃、实时热门商品、实时 GMV。Spark/Flink 用于数据量达到千万级后的离线/实时大规模处理。**这部分只写方案，不写实现代码。**

------------------------------------------------------------------------

# 3. 技术栈

## 3.1 Python（P0）

-   Python 3.11+、Pandas、NumPy、SciPy、scikit-learn、mlxtend、joblib
-   PyMySQL、SQLAlchemy 2.x、Pydantic、FastAPI、Uvicorn

## 3.2 Python（P1 可选）

-   LightGBM、XGBoost

## 3.3 数据库（P0）

-   MySQL 8.x

## 3.4 缓存（P1）

-   Redis

## 3.5 消息队列 / 大数据（P2，仅设计）

-   Kafka、Spark、Flink

## 3.6 前端（P0）

-   Vue 3、Vite、TypeScript、Axios、Vue Router、ECharts、Element Plus

## 3.7 工程（P0）

-   Git、pytest、Docker、Docker Compose、Ruff、Black

------------------------------------------------------------------------

# 4. 项目目录

``` text
ecommerce-recommendation-platform/
│
├── backend/
│   ├── app/
│   │   ├── main.py
│   │   ├── core/{config.py,database.py,logging.py,exceptions.py}
│   │   ├── models/ ├── schemas/ ├── routers/ ├── services/
│   │   ├── repositories/ └── middleware/
│   ├── tests/
│   ├── requirements.txt
│   └── .env.example
│
├── analysis/
│   ├── data_generation/
│   ├── cleaning/
│   ├── etl/
│   ├── quality/
│   ├── analysis/{user,item,funnel,retention,cohort,path,lifecycle,channel,price,association}/
│   ├── feature_engineering/
│   ├── models/{purchase_prediction,churn_prediction,ctr_prediction(P1)}/
│   ├── recommendation/
│   │   ├── base.py, popular.py, item_cf.py, user_cf.py, content_based.py
│   │   ├── hybrid.py, recall.py(P1), ranking.py(P1), evaluator.py
│   └── requirements.txt
│
├── data/{raw,interim,processed,features}/  + README.md
├── sql/{schema.sql,indexes.sql,views.sql,seed.sql}
│
├── frontend/
│   ├── src/{api,components,views,router,stores,types,utils}/
│   ├── package.json
│   └── vite.config.ts
│
├── tests/
├── docs/{architecture.md,database.md,analysis.md,recommendation.md,machine_learning.md,api.md,interview.md}
├── scripts/{init_db.py,generate_data.py,run_etl.py,run_all.py}
│
├── docker/
├── .env.example
├── .gitignore
├── docker-compose.yml
├── README.md
└── LICENSE
```

------------------------------------------------------------------------

# 5. 数据体系设计

必须包含（P0）：

``` text
users
categories
items
user_behaviors
orders
order_items
```

建议增加（P0，用于画像/分群/推荐落库）：

``` text
user_profile
item_profile
user_segments
recommendation_results
model_predictions
model_metrics
```

------------------------------------------------------------------------

# 6. 用户表 users

``` sql
id BIGINT PRIMARY KEY
user_id VARCHAR(32) UNIQUE NOT NULL
age INT
gender VARCHAR(16)
city VARCHAR(64)
register_time DATETIME
created_at DATETIME
updated_at DATETIME
```

要求：user_id 唯一；年龄合理；注册时间合理；城市分布不能全部相同。

------------------------------------------------------------------------

# 7. 分类表 categories

``` sql
id BIGINT PRIMARY KEY
category_id VARCHAR(32) UNIQUE NOT NULL
category_name VARCHAR(128)
parent_id VARCHAR(32)
```

至少生成：手机数码、电脑办公、家用电器、服饰、鞋包、美妆、食品、家居、运动户外、图书。

------------------------------------------------------------------------

# 8. 商品表 items

``` sql
id BIGINT PRIMARY KEY
item_id VARCHAR(32) UNIQUE NOT NULL
item_name VARCHAR(255)
category_id VARCHAR(32)
brand VARCHAR(128)
price DECIMAL(10,2)
stock INT
status TINYINT
created_at DATETIME
```

商品必须有不同热度等级（热门/普通/冷门），且不同分类的商品价格分布不同。

------------------------------------------------------------------------

# 9. 用户行为表 user_behaviors

``` sql
id BIGINT PRIMARY KEY
behavior_id VARCHAR(64) UNIQUE NOT NULL
user_id VARCHAR(32)
item_id VARCHAR(32)
behavior_type VARCHAR(16)
event_time DATETIME
event_date DATE
event_hour TINYINT
device_type VARCHAR(32)
channel VARCHAR(32)
```

-   行为：pv / click / collect / cart / buy
-   渠道：organic / search / ads / campaign / recommendation
-   设备：mobile / pc / tablet

------------------------------------------------------------------------

# 10. 订单表 orders

``` sql
id BIGINT PRIMARY KEY
order_id VARCHAR(64) UNIQUE NOT NULL
user_id VARCHAR(32)
order_time DATETIME
total_amount DECIMAL(12,2)
status VARCHAR(32)
payment_method VARCHAR(32)
```

状态：paid / cancelled / refunded

------------------------------------------------------------------------

# 11. 订单明细 order_items

``` sql
id BIGINT PRIMARY KEY
order_id VARCHAR(64)
item_id VARCHAR(32)
quantity INT
unit_price DECIMAL(10,2)
amount DECIMAL(12,2)
```

------------------------------------------------------------------------

# 12. 数据量规划

建议最终规模：

``` text
用户：10,000        分类：10~30
商品：5,000         用户行为：500,000~2,000,000
订单：50,000~150,000  订单明细：100,000~300,000
```

低配置电脑：

``` text
用户：2,000   商品：1,000   行为：100,000   订单：10,000
```

代码必须支持配置数据量（这也是 Kafka/Spark 被移入 P2 的直接依据——该量级下单机 Pandas/MySQL 已足够）。

------------------------------------------------------------------------

# 13. 模拟数据必须具有业务规律

禁止完全随机生成。

-   **用户偏好**：每个用户随机分配 1~3 个偏好类别。
-   **商品热度**：热门商品得到更高曝光概率。
-   **行为链**：购买必须与前置行为存在概率关联（PV → Click → Collect/Cart → Buy）。
-   **时间规律**：晚间活跃度较高，周末活跃度略高。
-   **用户价值**：至少生成高/中/低价值用户。
-   **渠道差异**：不同渠道设置不同的点击率、转化率、用户质量，渠道分析才有意义。

> 局限性提示（写入 docs/analysis.md）：所有"业务发现"均来自模拟规则本身，本质上是对生成规则的验证而非真实业务洞察，面试时应如实说明这是模拟数据集，重点展示方法论而非结论本身。

------------------------------------------------------------------------

# 14. 数据质量系统

必须检查：

-   **完整性**：user_id / item_id / event_time 是否为空
-   **唯一性**：behavior_id / order_id 是否重复
-   **一致性**：行为中的 user_id / item_id 是否存在于 users / items
-   **合法性**：behavior_type 是否属于允许集合；price >= 0；quantity > 0
-   **时间**：事件时间不能早于用户注册时间；订单时间不能晚于数据结束时间

输出 `data_quality_report.json`：

``` json
{
  "total_rows": 100000,
  "duplicate_rows": 120,
  "missing_rows": 32,
  "invalid_rows": 15,
  "final_rows": 99833
}
```

------------------------------------------------------------------------

# 15. ETL 流程

``` text
Raw → Schema Validation → Cleaning → Transformation → Quality Check → Feature Generation → MySQL
```

要求：批量插入；支持重复执行；支持输出进度；大文件使用 chunksize；禁止逐行写数据库。

------------------------------------------------------------------------

# 16. 基础数据分析

## 16.1 用户规模

指标：总用户 / 新增用户 / 活跃用户 / 购买用户 / 付费率

## 16.2 DAU / WAU / MAU

``` text
DAU = 当日产生至少一次行为的用户数
WAU = 当周产生行为的去重用户数
MAU = 当月产生行为的去重用户数
```

分析：趋势、环比、同比（模拟数据有意义时）、活跃用户占比

## 16.3 用户行为分析

统计 PV/Click/Collect/Cart/Buy，计算点击率/收藏率/加购率/购买率

## 16.4 活跃时间分析

按 hour / weekday 统计行为量，回答：用户什么时候最活跃？哪些时间购买最多？PC 和移动端活跃时间是否不同？

------------------------------------------------------------------------

# 17. GMV 分析

指标：GMV、订单数、购买用户数、客单价、ARPU、付费率

``` text
GMV = 有效支付订单金额之和
客单价 = GMV / 支付订单数
ARPU = GMV / 活跃用户数
```

按日/周/月分析 GMV 趋势、订单趋势、购买用户趋势、客单价趋势。

------------------------------------------------------------------------

# 18. 商品与分类分析

## 18.1 商品分析

指标：PV/Click/Collect/Cart/Buy/GMV/Unique Users/Conversion Rate；输出 TOP10/TOP20 商品、分类排行、品牌排行。

## 18.2 商品转化率

``` text
click_rate = click / pv
collect_rate = collect / pv
cart_rate = cart / pv
buy_rate = buy / pv
```

所有除法必须防止除零。

## 18.3 分类分析

按分类统计用户数/PV/点击/收藏/加购/购买/订单/GMV/转化率。

## 18.4 品牌分析

按品牌统计销量/GMV/用户数/复购用户/客单价。

------------------------------------------------------------------------

# 19. 转化漏斗

标准漏斗：PV → Click → Collect → Cart → Buy

输出：stage / count / step_conversion_rate / overall_conversion_rate，需分析每一步损失。

------------------------------------------------------------------------

# 20. 留存与 Cohort

## 20.1 用户留存

实现次日/3日/7日/14日/30日留存，按 cohort 计算。

## 20.2 Cohort 分析

以用户首次活跃/注册日期为 cohort，生成 cohort_date / day_0 / day_1 / day_3 / day_7 / day_14 / day_30，前端用热力图展示。

------------------------------------------------------------------------

# 21. 用户生命周期

定义阶段：新用户 / 成长期 / 活跃用户 / 高价值用户 / 沉默用户 / 流失风险

规则必须配置化，例如：

``` text
最近7天有行为 → 活跃
7~30天无行为 → 沉默
30天以上无行为 → 流失风险
```

不要把阈值散落在代码里，统一放入配置文件。

------------------------------------------------------------------------

# 22. RFM

``` text
R = 最近购买距分析日天数
F = 分析周期购买次数
M = 分析周期消费金额
```

评分 1~5，生成 R_score / F_score / M_score / RFM_score / segment。

分群：高价值 / 重要保持 / 潜力 / 一般 / 沉睡 / 流失风险

------------------------------------------------------------------------

# 23. 用户画像与用户分群

## 23.1 用户画像 user_profile

包括：基础属性、行为统计、购买统计、消费能力、活跃时间、偏好类别、偏好品牌、RFM、生命周期、渠道、设备。

## 23.2 用户分群

除 RFM 外，进阶实现 KMeans。

特征：total_pv / total_click / total_buy / total_amount / avg_order_amount / purchase_frequency / recency

输出 cluster_id / cluster_name，需要对聚类结果进行业务解释。

------------------------------------------------------------------------

# 24. 用户购买路径分析

分析典型路径，例如：

``` text
PV → Click → Cart → Buy
PV → Click → Collect → Cart → Buy
PV → Search → Click → Buy
```

统计路径数量、用户数量、最终购买率。

> 注意：数据只有页面行为时，不要伪造不存在的页面类型。如果没有 `search` 行为，就不要生成"搜索路径"。

------------------------------------------------------------------------

# 25. 商品生命周期

按时间窗口统计商品曝光/点击/加购/购买/GMV，判断新品/成长/爆款/成熟/衰退。建议使用最近 N 个窗口的趋势斜率和销量变化。

------------------------------------------------------------------------

# 26. 商品关联规则

使用 Apriori / FP-Growth。

输入：order_id → item_id 集合

指标：Support / Confidence / Lift（例如：手机 → 手机壳，笔记本 → 鼠标）

规则必须来自真实生成的数据。

------------------------------------------------------------------------

# 27. 价格分析

``` text
价格区间 → 点击率 → 加购率 → 购买率 → GMV
```

价格区间可自动分箱。同时分析价格 vs 购买频率、价格 vs 用户价值、价格 vs 转化率。

------------------------------------------------------------------------

# 28. 渠道与设备分析

## 28.1 渠道分析

渠道：organic / search / ads / campaign / recommendation

分析：用户数、新用户、活跃率、点击率、购买率、GMV、客单价、用户生命周期、用户长期价值。

> 如果没有广告成本字段，不要直接声称是真实 ROI，只能称为"渠道质量对比"。

## 28.2 设备分析

设备：mobile / pc / tablet

比较：活跃、转化、GMV、客单价、使用时间。

------------------------------------------------------------------------

# 29. 特征工程体系

## 用户特征

``` text
age, gender, city, total_pv, total_click, total_collect, total_cart, total_buy,
total_amount, avg_order_amount, recency, frequency, monetary,
favorite_category, favorite_brand, active_days
```

## 商品特征

``` text
price, category, brand, pv, click, collect, cart, buy, gmv,
conversion_rate, unique_users
```

## User-Item 特征

``` text
user_item_pv, user_item_click, user_item_collect, user_item_cart, user_item_buy,
last_interaction, interaction_count, category_preference, brand_preference, price_preference
```

------------------------------------------------------------------------

# 30. 训练集与测试集（防数据泄漏，强制规则）

所有预测模型和推荐评估都必须遵循时间原则：

``` text
前80%时间 → train
后20%时间 → test
```

禁止随机拆分导致未来数据进入训练集。

------------------------------------------------------------------------

# 31. 购买预测模型（P0）

目标：预测用户未来7天是否购买。标签：未来7天发生购买 = 1，否则 = 0。

模型依次：Logistic Regression → Random Forest →（P1）XGBoost/LightGBM

评估必须：Accuracy / Precision / Recall / F1 / ROC-AUC /（可选）PR-AUC / Confusion Matrix

> 如果类别不平衡：不允许只看 Accuracy，必须重点分析 Precision / Recall / F1 / AUC。

------------------------------------------------------------------------

# 32. 用户流失预测（P0）

定义：在观察窗口内活跃，但未来 30 天没有关键行为/购买。

模型：Logistic Regression / Random Forest /（P1）LightGBM

特征：最近行为距今天数、活跃天数、购买次数、最近购买金额、平均购买间隔、浏览次数、加购次数、收藏次数

输出：user_id / churn_probability / risk_level（low/medium/high），前端展示高风险用户列表。

------------------------------------------------------------------------

# 33. 用户价值预测（P1）

预测未来30天消费金额。可用 Linear Regression / Random Forest Regressor / LightGBM Regressor。

指标：MAE / RMSE / R²

------------------------------------------------------------------------

# 34. CTR 预测（P1）

目标：用户看到商品后是否点击。

特征：用户特征、商品特征、用户-商品特征、时间特征、渠道、设备

模型：Logistic Regression / LightGBM，输出 click_probability，用于推荐排序。

------------------------------------------------------------------------

# 35. 推荐系统总体设计（P0 核心 + P1 进阶）

推荐系统必须可插拔：

``` text
BaseRecommender
├── PopularRecommender        (P0)
├── ItemCFRecommender         (P0)
├── UserCFRecommender         (P0)
├── ContentBasedRecommender   (P0)
├── HybridRecommender         (P0)
└── TwoStageRecommender       (P1)
```

统一接口：

``` python
recommend(user_id: str, top_k: int = 10)
```

## 35.1 Popular 推荐

使用 PV/Click/Collect/Cart/Buy 综合计算热门度，必须标准化、时间衰减、权重可配置。

## 35.2 Item-CF

用户-商品矩阵权重：pv=1, click=2, collect=3, cart=4, buy=5

计算 item-item cosine similarity，流程：用户历史商品 → 相似商品 → 分数累加 → 过滤 → Top-K

## 35.3 User-CF

构建 user-user similarity，流程：目标用户 → 相似用户 → 相似用户喜欢的商品 → 过滤 → 加权 → Top-K

## 35.4 Content-Based

使用 category / brand / price_range / item tags，转换为向量（One-Hot / TF-IDF），计算 cosine similarity。

## 35.5 Hybrid 推荐

融合 ItemCF / UserCF / Popular / Content / UserPreference，统一先归一化到 [0,1]：

``` text
HybridScore = w1*ItemCF + w2*UserCF + w3*Popular + w4*Content + w5*Preference
```

所有权重配置化。

## 35.6 冷启动

-   新用户：Popular + 分类热门
-   少行为用户：Popular + ItemCF
-   新商品：Content-Based + 分类热门

## 35.7 推荐过滤

过滤：已购买 / 已推荐 / 下架 / 不存在 / 重复。必要时增加类别/品牌/价格区间多样性（P1）。

## 35.8 推荐解释

每条推荐必须尽量提供 reason，例如"与你最近浏览的商品相似""与你购买过的商品经常一起购买"。**不能对没有证据的推荐生成虚假解释。**

------------------------------------------------------------------------

# 36. 推荐评估（P0）

必须实现：Precision@K / Recall@K / F1@K / HitRate@K / NDCG@K / Coverage /（P1）Diversity

比较：Popular / ItemCF / UserCF / Content / Hybrid

最终输出表：Algorithm | Precision@10 | Recall@10 | F1@10 | HitRate@10 | NDCG@10 | Coverage

离线评估原则：历史行为 → train，未来行为 → test；推荐只能使用 train 信息，test 只用于评价。

------------------------------------------------------------------------

# 37. 多路召回与排序模型（P1，进阶）

``` text
User → [ItemCF | Popular | Content] → Candidate Set → Filtering
     → Feature Engine → Ranking(LightGBM) → Top-K
```

排序模型输入：user/item/user-item/context 特征，目标 click 或 buy，输出排序分数。

推荐系统最终架构（P0 到 P1 演进路径）：

``` text
User → Recall(Popular/ItemCF/UserCF/Content) → Candidate Pool → Deduplication
     → Filtering → Feature Engineering →（P1: Ranking Model）→ Diversity(P1) → Top-K → Explanation
```

------------------------------------------------------------------------

# 38. FastAPI 总体接口（P0）

统一前缀 `/api`

``` http
GET /api/health

GET /api/dashboard/overview
GET /api/dashboard/user-trend
GET /api/dashboard/gmv-trend
GET /api/dashboard/behavior-trend
GET /api/dashboard/funnel
GET /api/dashboard/retention

GET /api/users
GET /api/users/{user_id}
GET /api/users/{user_id}/profile
GET /api/users/{user_id}/behaviors
GET /api/users/{user_id}/orders
GET /api/users/{user_id}/recommendations

GET /api/items
GET /api/items/{item_id}
GET /api/items/ranking
GET /api/items/{item_id}/statistics

GET /api/analysis/rfm
GET /api/analysis/lifecycle
GET /api/analysis/cohort
GET /api/analysis/path
GET /api/analysis/channel
GET /api/analysis/price
GET /api/analysis/association

GET /api/models/purchase
GET /api/models/churn
GET /api/models/metrics

GET /api/recommendations/{user_id}
GET /api/recommendations/{user_id}/compare
GET /api/recommendations/metrics
```

## API 分层（强制）

``` text
Router → Service → Repository → Database
```

算法链路：

``` text
Router → RecommendationService → Recommender → Model / Cache / Repository
```

禁止把业务逻辑全部写在 Router。

------------------------------------------------------------------------

# 39. 前端页面（P0）

至少：`/dashboard` `/users` `/users/:id` `/items` `/items/:id` `/analysis` `/recommendations` `/models`

## 39.1 Dashboard

KPI：用户数、活跃用户、购买用户、订单数、GMV、客单价

图表：DAU趋势、GMV趋势、用户行为趋势、商品TOP10、分类TOP10、漏斗、留存热力图、渠道表现

## 39.2 用户分析页面

用户数量、活跃趋势、城市分布、RFM分群、生命周期、用户价值、高风险流失用户

## 39.3 用户详情页

基础信息、行为统计、购买统计、RFM、生命周期、偏好分类、偏好品牌、最近行为、推荐商品、购买预测、流失概率

## 39.4 商品分析页面

商品排行、分类排行、品牌排行、价格分布、转化率、生命周期、关联商品

## 39.5 推荐页面

允许输入 user_id，可选算法 Popular/ItemCF/UserCF/Content/Hybrid/（P1）TwoStage，展示推荐商品、分数、排名、推荐原因，并支持算法对比。

## 39.6 模型页面

购买预测（AUC/Precision/Recall/F1）、流失预测（同上）、推荐（Precision@10/Recall@10/NDCG@10/HitRate@10/Coverage）

## 39.7 ECharts

至少 Line / Bar / Pie / Funnel / Heatmap / Scatter /（可选）Radar，所有图表必须来自 API，禁止用写死数组作为最终展示数据。

------------------------------------------------------------------------

# 40. Redis（P1）

用途：Dashboard KPI、热门商品、用户画像、推荐结果、模型预测缓存

Key 示例：`dashboard:overview` `ranking:items:hot` `user:profile:U001` `recommend:user:U001` `prediction:churn:U001`，设置合理 TTL。

------------------------------------------------------------------------

# 41. Kafka / Spark / Flink（P2，仅设计，不实现）

**这一节的输出物是文档，不是代码。**

在 `docs/architecture.md` 中说明：

-   实时链路设计：`Frontend/Simulator → Kafka(topic: user_behavior, order_event) → Consumer → Redis → FastAPI → Dashboard`，实时指标包括实时PV/实时活跃/实时热门商品/实时订单/实时GMV。
-   大数据演进设计：当行为数据达到千万级后，ETL/画像/离线特征/批量推荐可迁移至 Spark；实时行为分析/实时窗口统计/实时特征可采用 Flink。
-   明确写出"当前数据规模下为什么不实现"，以及"如果数据量增长到什么阈值会考虑引入"，这是面试第40题的标准答题素材。

------------------------------------------------------------------------

# 42. Docker（P0，仅 backend/frontend/mysql；Redis 为 P1 追加）

``` text
backend
frontend
mysql
```

（P1 追加：redis）

启动：`docker compose up -d`

------------------------------------------------------------------------

# 43. 配置管理

使用 `.env`：

``` env
APP_ENV=development

MYSQL_HOST=127.0.0.1
MYSQL_PORT=3306
MYSQL_DATABASE=ecommerce_recommendation
MYSQL_USER=root
MYSQL_PASSWORD=

REDIS_HOST=127.0.0.1
REDIS_PORT=6379
REDIS_PASSWORD=

RECOMMEND_TOP_K=10
```

禁止把密码写入代码。

------------------------------------------------------------------------

# 44. 日志

记录：API request / ETL / data quality / model training / model loading / recommendation / latency / errors

示例：`INFO recommendation user=U001 strategy=hybrid latency=12ms`

------------------------------------------------------------------------

# 45. 测试

-   **数据**：数据量、空值、重复、外键一致性
-   **分析**：DAU、GMV、漏斗、RFM、留存
-   **模型**：训练成功、预测输出、指标计算
-   **推荐**：Top-K、过滤、冷启动、ItemCF、UserCF、Hybrid
-   **API**：Health、Dashboard、User、Item、Recommendation

------------------------------------------------------------------------

# 46. 性能要求

## 数据库索引

建立 user_id / item_id / event_time / event_date / behavior_type / category_id / order_id 索引。

## Pandas

大文件使用 `pd.read_csv(..., chunksize=...)`

## 推荐

禁止"HTTP请求 → 重新训练推荐模型"。应为：离线训练 → 保存模型/相似度 → API加载。

------------------------------------------------------------------------

# 47. 模型文件与版本

使用 joblib 保存：`purchase_model.joblib` `churn_model.joblib` `item_similarity.joblib` `user_similarity.joblib`（P1: `ctr_model.joblib`）

同时保存 `feature_columns.json` `model_metadata.json`

版本目录建议：

``` text
models/
├── purchase/{v1,v2}/
├── churn/
└── recommendation/
```

metadata 示例：

``` json
{
  "model_name": "purchase_prediction",
  "version": "v1",
  "train_time": "2026-08-12",
  "features": 25,
  "metrics": { "f1": 0.71, "auc": 0.82 }
}
```

## 可复现性

必须记录随机种子、数据时间范围、特征版本、模型参数、训练时间、评估指标。统一使用 `RANDOM_STATE = 42`。

------------------------------------------------------------------------

# 48. 项目运行方式

``` bash
# 1. 创建环境
python -m venv .venv

# 2. 激活环境（Windows）
.venv\Scripts\activate
# Linux/macOS: source .venv/bin/activate

# 3. 安装依赖
pip install -r backend/requirements.txt
pip install -r analysis/requirements.txt

# 4. 初始化数据库
python scripts/init_db.py

# 5. 生成数据
python scripts/generate_data.py

# 6. ETL
python scripts/run_etl.py

# 7. 运行分析
python scripts/run_all.py

# 8. 启动后端
uvicorn backend.app.main:app --reload

# 9. 启动前端
cd frontend
npm install
npm run dev
```

------------------------------------------------------------------------

# 49. 全流程 Phase 设计与工期估算

> 工期为单人 + 代码生成LLM协助 的粗略参考值，供排期使用，不是硬性要求。总计 P0 约 6~8 周，P1 视时间追加 1~2 周，P2 仅写文档，约 0.5 天。

| Phase | 内容 | 优先级 | 估算工期 |
|---|---|---|---|
| 0 | 需求和架构、创建仓库/目录、README | P0 | 0.5 天 |
| 1 | Python + FastAPI 初始化、config、logging、数据库连接、health API | P0 | 1 天 |
| 2 | MySQL 建表（6张核心表 + 主键/唯一键/外键逻辑/索引） | P0 | 1 天 |
| 3 | 数据生成器（含业务规律模拟） | P0 | 2~3 天 |
| 4 | 数据质量 + ETL（raw → processed → MySQL，可重复执行） | P0 | 2 天 |
| 5 | 基础分析（用户规模/DAU/WAU/MAU/行为/活跃时间/商品/分类/GMV/漏斗） | P0 | 3 天 |
| 6 | 留存 + Cohort + RFM | P0 | 2 天 |
| 7 | 深度业务分析（生命周期/路径/商品生命周期/价格/渠道/设备/关联规则） | P0 | 3~4 天 |
| 8 | 特征工程（user/item/user-item/context 特征表） | P0 | 2 天 |
| 9 | 购买预测（LR → RF，含时间切分、评估、模型保存） | P0 | 2 天 |
| 10 | 流失预测（标签构建/特征/模型/评估/保存/API） | P0 | 2 天 |
| 11 | 推荐 Baseline（Popular + 评估框架） | P0 | 1.5 天 |
| 12 | Item-CF + User-CF（训练/相似度/推荐/过滤/评估） | P0 | 2.5 天 |
| 13 | Content-Based 推荐 | P0 | 1.5 天 |
| 14 | Hybrid 推荐（权重实验） | P0 | 1.5 天 |
| 15 | 推荐评估最终表（对比 baseline 是否被超越） | P0 | 1 天 |
| 16 | FastAPI 全量接口（Dashboard/User/Item/Analysis/Model/Recommendation） | P0 | 2~3 天 |
| 17 | Vue3 全量页面 | P0 | 4~5 天 |
| 18 | 测试与性能优化（单测/API测试/索引优化） | P0 | 2 天 |
| 19 | 最终文档（README/architecture/database/analysis/ml/recommendation/api/interview，含 P2 架构设计文档） | P0 | 1.5 天 |
| — | Redis 缓存接入 | P1 | 1.5 天 |
| — | 用户价值预测、CTR 预测 | P1 | 2~3 天 |
| — | 多路召回 + LightGBM 排序 | P1 | 2~3 天 |
| — | 推荐多样性 | P1 | 0.5 天 |
| — | Kafka/Spark/Flink 架构设计文档 | P2 | 0.5 天 |

**第一阶段（首次启动项目）只做 Phase 0 + Phase 1**，创建 backend/analysis/data/sql/frontend/tests/docs/scripts 目录，并实现 FastAPI + config + logging + database + health API：

``` http
GET /api/health
```

返回：

``` json
{ "code": 0, "message": "success", "data": { "status": "ok" } }
```

第一阶段禁止实现：数据库全部业务表、推荐算法、机器学习、前端 Dashboard、Redis。这些必须按 Phase 顺序实现。

------------------------------------------------------------------------

# 50. 业务发现模块

代码不能只输出数字，需要自动生成分析结论，例如：

``` text
发现1：晚间18~23点是主要活跃时间。
发现2：推荐渠道用户的购买转化率高于自然流量。
发现3：数码类商品拥有更高客单价。
发现4：高RFM用户贡献了较高比例GMV。
发现5：加购行为与最终购买存在较强关联。
```

这些文字必须由真实计算结果生成，不能固定写死；同时应在 docs/analysis.md 注明该发现基于模拟数据规则，说明其作为方法论演示而非真实业务结论的局限性。

------------------------------------------------------------------------

# 51. 项目最终验收清单

## 数据（P0）
- [ ] 数据自动生成 [ ] 数据质量检查 [ ] ETL [ ] MySQL [ ] 数据量可配置

## 基础分析（P0）
- [ ] DAU/WAU/MAU [ ] GMV [ ] 客单价 [ ] ARPU [ ] 漏斗 [ ] 商品排行 [ ] 分类排行

## 深度分析（P0）
- [ ] 留存 [ ] Cohort [ ] RFM [ ] 生命周期 [ ] 路径 [ ] 关联规则 [ ] 价格 [ ] 渠道 [ ] 设备

## 机器学习
- [ ] 购买预测（P0） [ ] 流失预测（P0） [ ] 用户价值预测（P1） [ ] CTR预测（P1）

## 推荐（P0 主体 + P1 进阶）
- [ ] Popular [ ] ItemCF [ ] UserCF [ ] Content [ ] Hybrid [ ] 冷启动 [ ] 推荐解释
- [ ] 多样性（P1） [ ] 多路召回（P1） [ ] 排序模型（P1）

## 工程
- [ ] FastAPI（P0） [ ] Vue3（P0） [ ] ECharts（P0） [ ] pytest（P0） [ ] logging（P0） [ ] Docker(backend/frontend/mysql)（P0）
- [ ] Redis（P1）
- [ ] Kafka/Spark/Flink 架构设计文档（P2，非代码）

------------------------------------------------------------------------

# 52. 秋招简历应该突出什么

不要把所有技术栈全部堆在简历上，建议最终形成三类亮点：

## 数据分析方向

``` text
基于 Python 构建电商用户行为分析平台，完成 DAU/GMV/漏斗/Cohort/RFM/生命周期等分析，
并通过用户路径、渠道及商品关联规则挖掘业务规律。
```

## 数据科学/算法方向

``` text
构建购买预测与流失预测模型，使用时间切分避免数据泄漏，
并实现 Popular、User-CF、Item-CF、Content-Based 及 Hybrid 多策略推荐。
```

## 数据开发方向

``` text
搭建 Python ETL 数据处理链路，使用 MySQL 完成数据存储，
通过 FastAPI 提供分析与推荐服务，并给出 Redis/Kafka 的缓存与实时数据处理扩展方案。
```

> 注意最后一条措辞是"给出扩展方案"而不是"已实现"，与 P2 的实际完成状态保持一致，避免简历与实际项目不符。

------------------------------------------------------------------------

# 53. 项目面试必须能讲清楚的问题

## 数据
1. 数据从哪里来？为什么自己生成模拟数据？
2. 如何保证模拟数据具有业务规律？数据量是多少？
3. 如何清洗？如何保证数据质量？ETL怎么设计？

## 分析
4. DAU/GMV怎么计算？漏斗怎么定义？留存怎么算？
5. Cohort/RFM是什么？为什么使用RFM？
6. 用户生命周期怎么定义？路径分析怎么做？商品生命周期怎么判断？
7. 关联规则中的Support/Confidence/Lift是什么？如何分析渠道质量？

## 机器学习
8. 购买预测标签怎么定义？为什么时间切分？什么是数据泄漏？
9. 类别不平衡怎么办？为什么不能只看Accuracy？AUC是什么？
10. 流失预测如何定义？哪些特征最重要？

## 推荐
11. Item-CF/User-CF是什么？两者区别？为什么使用Cosine？
12. 行为权重怎么设置？如何解决冷启动？如何过滤已经购买商品？
13. Hybrid为什么有效？Precision@K和Recall@K区别？NDCG@K/Coverage是什么？
14. 为什么推荐模型不能每次API请求重新训练？

## 架构演进（对应 P2）
15. **如果数据扩大100倍怎么办？**——这里正是 Kafka/Spark/Flink 设计文档发挥作用的地方：说明当前为何不用、阈值在哪、迁移路径是什么。

------------------------------------------------------------------------

# 54. 代码生成 LLM 的强制规则

1. **禁止一次性生成整个项目**：每次只完成一个 Phase。
2. **先检查现状**：如果已有代码，先读取 → 理解 → 检查 → 再修改，禁止盲目覆盖。
3. **每阶段必须运行**：生成代码后必须告诉用户运行什么、预期看到什么、如何验证。
4. **不允许伪实现**：禁止 `pass` / `return mock_data` / `return []` 作为核心业务实现。
5. **禁止前端写死分析数据**：所有最终数据必须 API 获取。
6. **算法必须真实**：禁止用随机数冒充模型效果。
7. **防止数据泄漏**：预测和推荐评估必须严格遵守时间切分。
8. **严格按优先级实现**：P0 未完成前不得开始 P1；P2 只产出设计文档，任何情况下都不编写 Kafka/Spark/Flink 的实现代码，防止范围蔓延。
9. **核心代码必须可解释**：每个核心模块说明输入、输出、算法、复杂度、业务意义。
10. **结果必须可复现**：使用 `random_state=42`，并记录数据版本、特征版本、模型版本、参数、指标。

------------------------------------------------------------------------

# 55. 开发过程中 LLM 的输出格式

每完成一个 Phase，必须输出：

1. 本阶段目标
2. 修改/新增文件
3. 完整代码
4. 运行命令
5. 验证方法
6. 常见错误
7. 下一阶段

不要在没有验证上一阶段的情况下直接跳到下一阶段。

------------------------------------------------------------------------

# 56. 最终能力矩阵

``` text
Python
├── 数据处理: Pandas / NumPy / SQL / ETL / 数据质量
├── 数据分析: DAU/MAU / GMV / Funnel / Retention / Cohort / RFM / Lifecycle / Path / Association / Channel
├── Machine Learning: Purchase Prediction / Churn Prediction /（P1）Value Prediction / CTR Prediction
├── Recommendation: Popular / ItemCF / UserCF / Content / Hybrid /（P1）Recall / Ranking
├── Backend: FastAPI / SQLAlchemy / RESTful API
├── Storage: MySQL /（P1）Redis
├── Streaming（P2，仅设计）: Kafka / Spark / Flink
├── Visualization: Vue3 / ECharts
└── Engineering: Git / pytest / Docker / Logging
```

------------------------------------------------------------------------

# 57. 项目最终闭环

``` text
数据 → 数据质量 → ETL → 数据仓库
  ↓
用户分析 / 商品分析 / 渠道分析
  ↓
深度分析（RFM / 生命周期 / 路径 / 关联规则 / 价格 / 设备）
  ↓
特征工程
  ↓
预测模型（购买/流失，P1:价值/CTR） ｜ 推荐系统（Popular/CF/Content/Hybrid，P1:召回/排序）
  ↓
模型评估
  ↓
FastAPI → MySQL /（P1）Redis
  ↓
Vue3 + ECharts
  ↓
可视化平台 → 业务决策/推荐
```

------------------------------------------------------------------------

# 58. 最终原则

这个项目的核心不是"用了多少框架"，而是：

> **能不能从一批原始数据出发，经过数据处理、分析、建模和推荐，最终形成一个真实可运行、可解释、可评估、可展示的数据应用——并且诚实地说明哪些部分是核心实现、哪些部分是仅作架构设计的扩展方向。**

最终应做到：

``` text
数据真实 + 指标正确 + 分析有业务意义 + 模型有评估 + 推荐有对比
+ 接口可调用 + 前端可展示 + 工程可运行 + 代码可解释 + 项目可面试
+ 范围诚实（P0/P1/P2 边界清晰，不夸大未实现部分）
```

这才是本项目的最终完成标准。
